# StoryLens 개발 로그

> 랄프 루프(Codex CLI 자동 반복 개발) 세션마다 아래 템플릿으로 기록을 append합니다.
> 상세 스펙은 `상세기획문서.md`를 참조하고, 여기서는 진행 상황·이슈·결정 사항만 남깁니다.

---

## 로그 작성 템플릿

```
## 세션 N — (세션 주제) (YYYY-MM-DD)
- 목표:
- 완료한 작업:
- 발견한 이슈/블로커:
- 변경된 파일:
- 다음 세션 TODO:
```

---

## 세션 0 — 기획 준비 (2026-07-21)

- **목표**: 공모전 요강 분석 및 MVP 스코프 확정, 상세기획문서.md 뼈대 작성
- **완료한 작업**:
  - 2026 AI 활용 공모전(나는 Solo AI) 요강 분석 — 1인 단독 참가, 제출 마감 7/31 14:00 확인
  - 심사기준 분석 — AI활용성(25)·완성도(25) 우선, 동점 시 AI활용성>완성도>창의성>활용가능성
  - 경쟁 서비스 조사 (Sudowrite, Novelcrafter, PlotLab) — 차별화 지점 확인
  - MVP 핵심 루프 확정: 입력 → 트로프 매핑/진단 → 구조 고정 생성 → 자체 검증 → Before/After 비교
  - 스코프 축소 결정: 트로프 3~4개, 데모는 고정 케이스 1개로 승부 (완성도 우선 전략)
  - 프로젝트명 확정: StoryLens
  - 상세기획문서.md 뼈대 작성 완료
- **발견한 이슈/블로커**:
  - 트로프 데이터(심리적 기능·서사 단계 매핑·판별 키워드)가 아직 미확정 — 구텐베르크 작품 조사 필요
  - 기술 스택 미확정
- **변경된 파일**:
  - `상세기획문서.md` (신규)
  - `로그.md` (신규)
- **다음 세션 TODO**:
  1. 구텐베르크 참조작 조사 → 트로프 3~4개 확정
  2. 상세기획문서.md 5번 표(트로프 데이터) 채우기
  3. 데모 고정 입력값 결정
  4. 기술 스택 결정 후 개발 착수

---

## 세션 1 — 구조 요소 태그 풀 확정 (2026-07-22)

- **목표**: 구텐베르크 참조 텍스트 및 트로프 데이터 확정
- **완료한 작업**:
  - 기존 조사 자료(신화 원형 매핑, Stella& Medium 시리즈, 구텐베르크 원문 링크) 과거 대화에서 재확인
  - 복수극 매핑 신규 조사 — 몬테크리스토 백작(#1184), 그리스 신화 네메시스/테미스·디케 연결
  - 크리스마스 캐럴(#46) 확정 — 후회/재도전 태그 (팀 버튼 "크리스마스의 악몽"은 저작권 문제로 제외 확인)
  - 장자 나비 꿈 추가 — 동양 전통 전체는 제외하되 단일 우화는 정체성 모호성 태그로 예외 포함
  - 아르센 뤼팽(#6133) 재해석 — 정체성 유동성/정보우위 태그로 재활용
  - 왕자와 거지(#1837) 신규 발굴 — 신분위장/정체성은닉 태그의 핵심 텍스트
  - 폭풍의 언덕(#768) 제외 결정 (로맨스 색채가 강해 구조 태그 연결 약함)
  - **트로프 분류 방식 전면 변경**: 회귀물/복수극/빙의물 배타적 카테고리 → "웹소설 트로프" 단일 축 + 구조 요소 태그 7개 조합 방식으로 확정 (실제 웹소설의 장르 혼합 특성 반영)
  - 상세기획문서.md 2·3·4·5·10번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (2, 3, 4, 5, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 데모 고정 입력값 선정 (여러 태그가 동시에 감지되는 케이스 권장)
  2. 태그 동시 감지 시 우선순위/가중치 로직 설계
  3. 기술 스택 확정
  4. 개발 착수

---

## 세션 2 — 영웅서사 태그 보강 (2026-07-22)

- **목표**: 누락된 "먼치킨/영웅서사" 태그 보완
- **완료한 작업**:
  - 헤라클레스 12과업 관련 구텐베르크 원문 조사 → "The Twelve Labours of Hercules"(#22529) 발굴 — 속죄를 위한 시련이라는 프레이밍 확인
  - 신규 태그 "영웅서사/시련과 희생" 추가 (전지적 독자 시점, 나 혼자만 레벨업 등 먼치킨물의 심층 구조와 연결되는 통찰 반영)
  - 구조 요소 태그 풀 7개 → 8개로 확정
  - 상세기획문서.md 4, 5번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (4, 5번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 데모 고정 입력값 선정 (여러 태그가 동시에 감지되는 케이스 권장)
  2. 태그 동시 감지 시 우선순위/가중치 로직 설계
  3. 기술 스택 확정
  4. 개발 착수

---

## 세션 3 — 학술적 근거 보강 (2026-07-22)

- **목표**: 국내 웹소설 학술 논문 검토 및 태그 풀 근거 보강
- **완료한 작업**:
  - 오태영(2022) 「웹소설 연구의 현황과 전망」 검토 — 메타 리뷰(연구사 정리) 논문으로 확인, 직접 매핑 자료로는 부적합하나 학술 포지셔닝용으로 채택
  - 고경은·조혜정(2022) 「한국 웹소설의 서사 모티프 유형 및 'AI 유저 반응' 연구」 검토 — 카카오페이지 116편 실증 분석, '회빙환' 모티프의 현실 보상 심리 확인 → 핵심 학술 근거로 채택
  - 관련 '시스템 모티프' 논문 추가 발견 — 정보우위/반전 태그 보강 자료 후보로 기록
  - 상세기획문서.md 2번, 5번(5-1 신설), 10번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (2, 5, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 데모 고정 입력값 선정 (여러 태그가 동시에 감지되는 케이스 권장)
  2. 태그 동시 감지 시 우선순위/가중치 로직 설계
  3. 기술 스택 확정
  4. 개발 착수

---

## 세션 4 — 메타인지/이중시선 태그 신설 (2026-07-22)

- **목표**: 조형래(2022) 논문 검토 및 누락된 태그(메타인지/이중시선) 보완
- **완료한 작업**:
  - 조형래(2022) 「웹소설의 사상: 『전지적 독자 시점』을 중심으로」 검토 — "플레이어 포지션"의 이중화된 시선(몰입+메타적 계산) 개념 확인
  - 기존 "정보우위/반전"(아르센 뤼팽) 태그와 구분되는 별도 태그로 판단, "메타인지/이중시선(플레이어 포지션)" 신설
  - 구조 요소 태그 8개 → 9개로 확정
  - 전지적 독자 시점 원문은 저작권 있는 현재 연재작이므로 참조 텍스트로 사용 불가 확인 — 논문의 개념만 판별 로직 근거로 채택 (저작권 이슈 명확히 구분)
  - 상세기획문서.md 4, 5, 5-1, 10번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (4, 5, 5-1, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 데모 고정 입력값 선정 (여러 태그가 동시에 감지되는 케이스 권장)
  2. 태그 동시 감지 시 우선순위/가중치 로직 설계
  3. 기술 스택 확정
  4. 개발 착수

---

## 세션 5 — 메타인지 태그 참조 텍스트 보강 (2026-07-22)

- **목표**: 메타인지/이중시선 태그의 퍼블릭 도메인 참조 텍스트 발굴 (기존엔 참조 텍스트 없이 논문 개념만 있었음)
- **완료한 작업**:
  - 사용자의 통찰 반영 — 메타인지적 이중시선은 웹소설에서 니치한 요소가 아니라 몰입 대비 전지적 쾌감/우월감을 만드는 핵심 서술 기제라는 관점 확인
  - "A Connecticut Yankee in King Arthur's Court"(마크 트웨인, Gutenberg #86) 발굴 — 현대인이 낯선 세계에서 자신만 아는 지식을 게임처럼 활용해 공략하는 구조, 이세계/시스템 웹소설의 서구 원형에 가장 근접
  - 상세기획문서.md 5, 5-1번 섹션 갱신 (참조 텍스트 공백 해소)
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (5, 5-1번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 데모 고정 입력값 선정 (여러 태그가 동시에 감지되는 케이스 권장)
  2. 태그 동시 감지 시 우선순위/가중치 로직 설계
  3. 기술 스택 확정
  4. 개발 착수

---

## 세션 6 — 데모 입력값 확정 (2026-07-22)

- **목표**: 고정 데모 입력값 확정
- **완료한 작업**:
  - "재벌집 막내아들" 서사 구조를 참고 케이스로 채택 (캐릭터명·가문명·사업 설정 등은 저작권 문제 방지를 위해 전부 오리지널로 재구성)
  - 초안 입력에 "상황 설정"만 있고 "목표"가 없어 구조 고정 생성의 도착점이 없다는 점 발견 → 목표 문장("후계자 등극 + 배신자 응징") 추가로 보완
  - 입력 정의 자체에 "명시적 목표 포함 필수" 원칙 추가 (일반화된 설계 원칙으로 반영)
  - 최종 입력값 확정 — 5개 태그(복수/응징, 정보우위/반전, 사회적 지위 역전, 후회/재도전, 메타인지/이중시선) 동시 활성화, 신분위장/정체성은닉은 부차 감지
  - 목표 기반 서사 뼈대(상실→회귀·각성→목표설정→실행→응징) 초안 작성
  - 상세기획문서.md 3, 6, 10번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (3, 6, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계
  2. 기술 스택 확정
  3. Before/After 스크립트 초안 작성 (실제 생성 로직 구현 전 손으로 먼저 써보는 것도 고려)
  4. 개발 착수

---

## 세션 7 — 포맷/문체 규칙 및 few-shot 확보 (2026-07-22)

- **목표**: 챕터 단위 포맷/문체를 저작권 문제 없이 확보 (실제 웹소설 few-shot 대체안)
- **완료한 작업**:
  - 최배은(2017) 「한국 웹소설의 서술형식 연구」 검토 — 행동/대사 중심 서술, 시공간 묘사 생략 경향 확인
  - 안상원(2017) 「웹소설 유료화에 따른 플랫폼과 서사의 변화 양상 연구」 검토 — 클리셰 반복·변주 중심 연재 리듬 확인
  - 기존 확보한 시스템 모티프 논문(5-1)과 종합해 포맷 규칙 4가지로 정리 (문단/서술, 문단 길이, 상태창 표기, 회차 리듬)
  - 실제 작품 인용 없이, 6번 데모 입력값을 기반으로 규칙을 적용한 **오리지널 예시 문단** 직접 작성 (저작권 문제 없는 few-shot으로 사용 가능)
  - 상세기획문서.md 4번, 5-2(신설) 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (4, 5-2번 섹션 수정/신설)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계
  2. 기술 스택 확정
  3. Before/After 스크립트 초안 작성
  4. 개발 착수

---

## 세션 8 — 플롯 리듬/구조 규칙 보강 (2026-07-22)

- **목표**: 플롯 구조(회차/전개 리듬) 관련 학술·실무 근거 확보
- **완료한 작업**:
  - 기존 인용한 최배은(2017) 논문에서 놓쳤던 핵심 문장 재발견 — "플롯은 작품 전체의 유기성보다 매 연재분의 흥미와 긴장감에 좌우된다"는 서술을 학술 근거로 채택
  - 사이다/고구마 리듬 개념 조사 — 나무위키·실무 작법 가이드에 문서화된 업계 관습으로 확인, 정식 KCI 학술논문 근거는 확인되지 않아 "실무 관습 참고"로 별도 구분 표기
  - 관련 KCI 논문 추가 조사(웹소설 장르 체계의 심급, 이융희의 웹소설 연구 등) — 직접 채택할 만한 것은 없었으나 향후 확장 후보로 인지
  - 상세기획문서.md 3번, 5-3(신설) 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (3, 5-3번 섹션 수정/신설)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계
  2. 기술 스택 확정
  3. Before/After 스크립트 초안 작성
  4. 개발 착수

---

## 세션 9 — 참고자료 목록 통합 정리 (2026-07-22)

- **목표**: 지금까지 조사한 모든 자료를 카테고리별로 정리해 문서 말미에 삽입 (사용자의 추가 연구 목적)
- **완료한 작업**:
  - 국내 학술논문(핵심 채택 6건 + 미채택 참고용 6건), 해외 학술논문(8건), 웹 아티클/블로그(6건), 구텐베르크 원문(12건) 전체 목록화
  - 상세기획문서.md 11번(신설) 섹션 추가
  - "시스템 모티프 분석 논문"의 서지 정보가 불완전하다는 점 명시 — 추가 재검색 필요 항목으로 플래그
- **발견한 이슈/블로커**: 시스템 모티프 논문의 정확한 저자/게재지 미확인 (사용자 재검색 권장 사항으로 문서에 명시)
- **변경된 파일**:
  - `상세기획문서.md` (11번 섹션 신설)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계
  2. 기술 스택 확정
  3. Before/After 스크립트 초안 작성
  4. 개발 착수

---

## 세션 10 — 서지 확정 및 마스터플롯 연구 발견 (2026-07-22)

- **목표**: 시스템 모티프 논문 서지 확정, 플롯 구조 관련 추가 학술 근거 확보
- **완료한 작업**:
  - 사용자가 "시스템 모티프 분석" 논문 저자 확인 — 서재인(2025), 대중서사연구 31(2), DOI: 10.18856/jpn.2025.31.2.012로 서지 확정, 11-1/5-1/5-2 전부 갱신
  - 관련 검색 중 핵심 논문 추가 발견:
    - 안상원(2018), 「한국 장르소설의 마스터플롯 연구—모험서사의 변이로 본 '차원이동' 연구」(국어국문학 184) — 회귀·빙의·환생·접속을 "차원이동"이라는 하나의 마스터플롯으로 통합, 핵심 개념은 "인정욕망". **플롯 구조(5-3)의 중심 학술 근거로 채택**
    - 안상원, 「한국 웹소설의 회귀 모티프 연구」(한국문학이론과 비평 22(3)) — 후회/재도전 태그의 더 정밀한 근거로 채택 (5-1)
    - 김하나(2025), 「한국 웹소설의 '시스템' 기반 '인생 리셋'과 '설정화'하는 이야기 환경」(대중서사연구 31(1)) — 후회/재도전+메타인지 교차 지점 보강 자료로 채택 (5-1)
  - 안상원 저자의 추가 논문 3건(책빙의물 특성, 한일 마스터플롯 비교, 대체역사물 연구)을 11-2 미채택 목록에 추가 — 동일 저자가 반복적으로 유의미한 결과를 냈으므로 향후 확장 시 우선 검토 대상
  - 상세기획문서.md 5-1, 5-3, 11-1, 11-2번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (5-1, 5-3, 11-1, 11-2번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계
  2. 기술 스택 확정
  3. Before/After 스크립트 초안 작성
  4. 개발 착수

---

## 세션 11 — 기술 스택 확정 (2026-07-22)

- **목표**: 백엔드/프론트엔드 기술 스택 확정
- **완료한 작업**:
  - 백엔드: Spring Boot + Spring AI 확정 — Game QA Copilot의 Actor-Evaluator 루프 재사용 가능, 포트폴리오 일관성(Spring AI 교육과정·기존 프로젝트와 연속성) 고려
  - Spring AI의 국내 대중성(LangChain 대비) 우려 검토 — 공모전 심사기준엔 영향 없음 확인, 포트폴리오 측면에서도 국내 기업(SK 데보션, 사이오닉AI 등)의 Spring AI 활용 사례 존재로 오히려 일관된 강점 서사로 판단
  - 프론트엔드: React + Tailwind CSS + Framer Motion 확정 — 1차 심사부터 시연 영상 기반이라는 점을 반영, 코드 품질보다 화면 연출에 투자 우선순위를 두기로 결정
  - 진단 결과 화면을 RPG 상태창 스타일로 디자인하기로 결정 (5-2 규칙을 시각 디자인에도 연동, 메타인지 태그 컨셉을 UI로 증명)
  - 투자 우선순위 원칙 확정: 데모 3분 영상에 등장하는 화면(입력/진단결과/Before-After)에만 집중
  - 상세기획문서.md 7, 10번 섹션 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (7, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 태그 동시 감지 시 우선순위/가중치 로직 설계 (마지막 미해결 항목)
  2. Before/After 스크립트 초안 작성
  3. 출품작 한 줄 소개 문구 확정
  4. 개발 착수

---

## 세션 12 — 태그 진단 로직 확정 (2026-07-22)

- **목표**: 태그 동시 감지 시 스코어링 방식 및 우선순위/가중치 로직 확정 (마지막 미해결 항목)
- **완료한 작업**:
  - 스코어링 방식 3가지 옵션 비교: A) 키워드 매칭, B) 임베딩 의미 유사도, C) LLM 프롬프트 기반 분류
  - 마감일(7/31)까지는 서류·시연영상만 필요하다는 점을 고려해, **옵션 C를 MVP로 채택**하고 임베딩(옵션 B)은 MVP 이후 확장 로드맵으로 분리하기로 결정
  - 우선순위/가중치 규칙 확정: 임계값 이상 활성 태그 선별 → 상위 1~2개 주동력(안상원 6단계 마스터플롯 골격의 핵심 갈등 규정) / 나머지는 보조 색채로 구분 → 동점 시 사용자 목표 문장과의 연관성으로 판별
  - 이 결정을 8번 심사기준(확장성)의 구체적 근거로도 연결
  - 상세기획문서.md 3-1(신설), 8, 10번 섹션 갱신
  - **10번 섹션의 모든 미해결 항목 체크 완료** — 트로프 확정, 데모 입력값, 기술 스택, 태그 우선순위 로직, 마지막 남은 건 출품작 한 줄 소개 문구뿐
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (3-1 신설, 8, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 출품작 한 줄 소개 문구 확정 (마지막 미해결 항목)
  2. Before/After 스크립트 초안 작성
  3. 개발 착수 (랄프 루프 세션 시작)

---

## 세션 13 — 태그 배치 로직 재설계 (2026-07-22)

- **목표**: 세션12에서 확정한 "주동력/보조색채" 우선순위 방식이 프로젝트 핵심 원칙과 모순됨을 발견, 재설계
- **완료한 작업**:
  - 사용자 피드백으로 문제 발견: "상위 1~2개만 주동력, 나머지는 장식"이라는 규칙이 "웹소설은 장르가 짬뽕되어 있고 모든 요소가 중요하다"는 2번 섹션의 핵심 설계 철학(배타적 트로프 분류 폐기 이유)과 정면으로 모순됨
  - 재설계: 순위 기반(누가 더 중요한가) → **위치 기반(각 태그가 마스터플롯 어느 단계를 담당하는가)** 로 전환
  - 9개 태그 전체에 대해 "주로 작동하는 단계" 매핑표 작성 (5단계 뼈대: 상실→회귀·각성→목표설정→실행→응징·도달 기준)
  - 한 단계에 여러 태그가 겹칠 경우 순위를 매기지 않고 함께 녹여내는 규칙으로 설계
  - 6번 데모 케이스에 재설계된 로직 적용 — 5개 활성 태그 전부가 뼈대의 각기 다른 단계에서 실제 구조적 역할을 갖도록 재배치 확인
  - 상세기획문서.md 3-1, 6, 10번 섹션 갱신
- **발견한 이슈/블로커**: 없음 (오히려 이전 세션의 설계 결함을 사용자가 짚어줘서 개선)
- **변경된 파일**:
  - `상세기획문서.md` (3-1, 6, 10번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 출품작 한 줄 소개 문구 확정 (마지막 미해결 항목)
  2. Before/After 스크립트 초안 작성 (재설계된 단계 배치 로직 반영)
  3. 개발 착수 (랄프 루프 세션 시작)

---

## 세션 14 — 외부 피드백 반영 (일정 수정·기능유형·검증체계 보강) (2026-07-22)

- **목표**: 외부 검토 피드백(8개 항목) 검토 후 반영
- **완료한 작업**:
  - **일정 오류 수정 (긴급)**: 상세기획문서.md 9번에 "오늘 2026-07-21"로 잘못 기재되어 있던 것을 발견 — 실제 오늘은 7/22, 마감까지 9일 남음으로 정정. 날짜별 세부 일정으로 재작성 (7/24 API연동, 7/26 루프완성, 7/29 기능동결 등)
  - **9-1 내부 QA 테스트 케이스 신설**: 공개 데모 외 3개 유형(회귀+복수+정보우위 / 빙의+신분위장+낯선세계 / 메타인지+영웅서사) 내부 검증 항목 추가
  - **태그 3층 구조 도입**: 9개 태그를 서사모티프/전개기능/전역제약·주제로 재분류, 5번 태그 테이블에 "층위" 컬럼 추가 — "왜 복수와 시점기법이 같은 층위냐"는 질문 방어
  - **점수 표기 방식 변경**: "+30" 가산점 표기 → "강하게 감지 + 근거 문구" 정성적 표기로 전환 (부산 챗봇 논의 때와 동일한 "82점 근거가 뭐죠" 함정 재확인 및 회피)
  - **3-2 신설 (기능유형 재분류 + 자유 창작 영역)**: 활성 태그를 핵심목표/필수사건/지속조건/톤조건 4가지로 재분류해 "고정 조건"으로 삼고, 그 외 장면 연출·대사·디테일은 "자유 창작 영역"으로 명시적으로 분리 — 사용자가 제기한 "과도한 제약 시 매번 같은 결과물이 나오는 반복성 문제"에 대한 해결책으로 도입
  - **3-3 신설 (Actor-Evaluator 체크리스트 명세)**: 자기 채점 위험을 막기 위해 추상적 평가("아크와 일치하는가?") 대신 7개 체크리스트 + PASS/FAIL·evidence·violated_constraint·correction_instruction JSON 출력 형식으로 구체화
  - **3-4 신설 (Before/After 공정성 원칙)**: 같은 모델·입력·출력길이·temperature 통제 원칙 명시. 실패 시나리오는 기존 6번 내용이 이미 원칙에 부합함을 확인
  - **한 줄 소개 문구 확정**: "웹소설 설정을 분석해 이야기가 산으로 가지 않도록 지켜주는 AI 창작 가드레일" — "AI 창작 도우미"보다 "AI 서사 가드레일" 포지셔닝 채택 (마지막 미해결 항목 해소)
  - 6번 데모 시나리오에 기능유형/자유창작영역 예시, 새 점수 표기 방식 반영
  - 작업지침.md 스코프 가드레일에 4개 원칙 추가, 세션 계획을 날짜 기준으로 전면 교체
- **발견한 이슈/블로커**: 없음 (외부 피드백이 실제 설계 결함과 일정 계산 오류를 다수 짚어줌 — 특히 일정 오류는 방치 시 실제 마감을 놓칠 위험이 있었음)
- **변경된 파일**:
  - `상세기획문서.md` (1, 3, 3-1, 3-2 신설, 3-3 신설, 3-4 신설, 5, 6, 9, 9-1 신설, 10번 섹션 수정)
  - `작업지침.md` (스코프 가드레일, 7번 세션 계획 수정)
  - `로그.md` (본 세션 기록 추가)
  - **10번 미해결 질문 전체 완료** — 더 이상 미결 항목 없음
- **다음 세션 TODO**:
  1. 개발 착수 (랄프 루프 세션 시작) — 작업지침.md 7번 일정표의 7/23 항목부터
  2. Before/After 스크립트 초안 작성 (3-4 공정성 원칙 반영)

---

## 세션 15 — 개발 착수 준비물 작성 (2026-07-22)

- **목표**: 랄프 루프 개발 착수 전 참조 자료 3종 준비 (tags.json, 시스템 프롬프트 3개, Before/After 초안)
- **완료한 작업**:
  - `tags.json` 작성 — 9개 태그를 id/tier/psychological_function/keywords/reference_text/master_plot_stage/typical_functional_type 필드로 구조화, 임계값(40점)·표시방식 메타데이터 포함
  - `prompts/01_tag_diagnosis_prompt.md` — 태그 진단(3-1) 시스템 프롬프트 + 6번 데모 케이스 기준 예상 출력 JSON
  - `prompts/02_structure_generation_prompt.md` — 구조 고정 생성(3-2) 시스템 프롬프트, 고정 조건/자유 창작 영역 분리, 5-2 포맷 규칙·few-shot 연동
  - `prompts/03_actor_evaluator_prompt.md` — 자체 검증(3-3) 체크리스트 7항목 + PASS/FAIL JSON 스키마, 재생성 루프 연동 방식 명시
  - `before_after_draft.md` — 6번 데모 케이스 기준 Before(이탈 예시)/After(5단계 고정 준수) 손글씨 초안 작성, 3-4 공정성 원칙(동일 도입부, 유사 길이) 적용, 차이 요약표 포함
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `tags.json` (신규)
  - `prompts/01_tag_diagnosis_prompt.md` (신규)
  - `prompts/02_structure_generation_prompt.md` (신규)
  - `prompts/03_actor_evaluator_prompt.md` (신규)
  - `before_after_draft.md` (신규)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 작업지침.md 7번 일정표 7/23 항목(프로젝트 스캐폴딩)부터, 위 5개 파일을 컨텍스트로 제공
  2. 개발 중 실제 LLM 출력이 before_after_draft.md 수준의 일관성을 보이는지 검증

---

## 세션 16 — 참조 서사 카드 도입 (narrative_cards.json) (2026-07-22)

- **목표**: 구텐베르크 원문 임베딩 필요성 검토 및 참조 데이터 구조 확정
- **완료한 작업**:
  - 외부 검토 의견 검토 — 원문 전체 청크 임베딩은 장면 레벨 유사성만 찾고 작품 전체 플롯 구조를 포착 못함(장편을 조각내면 서사구조가 사라짐)이라는 기술적 판단에 동의
  - 노션에 사용자가 이미 정리해둔 12개 작품 페이지(연결태그/작품요약/StoryLens 적용포인트/이용 시 주의) 전체 확인 — 제안된 "서사 카드" 스키마와 구조적으로 거의 일치함을 확인
  - `narrative_cards.json` 작성 — 12개 작품(핵심참조 8, 보조참고 2, 검토후제외 1, 개념참조 1)을 connected_tags/protagonist_goal/initial_condition/advantage_acquisition/story_beats/psychological_function/usage_constraints 필드로 구조화
  - MVP 방식 확정: 원문 임베딩 없이 **태그 기반 후보 필터링 + LLM 구조 비교**로 참조 작품 선정 (3-1-1 신설) — 세션12의 "임베딩보다 LLM 판단 우선" 원칙과 일관됨
  - 파이프라인 위치 확정: 3-1(태그진단)과 3-2(구조생성) 사이에 3-1-1 삽입
  - 임베딩 확장 로드맵 갱신: 카드가 수백 개로 늘어나면 "카드 임베딩"(원문 임베딩 아님) 도입 검토로 명시
  - 상세기획문서.md 3-1, 3-1-1(신설), 5번 섹션 갱신
  - prompts/02_structure_generation_prompt.md에 서사카드 선정을 전처리 0단계로 추가, 시스템 프롬프트에 [참조 서사 구조] 섹션 추가
  - 작업지침.md 스코프 가드레일에 원문 임베딩 금지 원칙 및 usage_constraints 확인 원칙 추가
- **발견한 이슈/블로커**: 없음 (오히려 사용자가 이미 해둔 노션 정리 덕분에 카드 작성이 매우 수월했음)
- **변경된 파일**:
  - `narrative_cards.json` (신규)
  - `상세기획문서.md` (3-1, 3-1-1 신설, 5번 섹션 수정)
  - `prompts/02_structure_generation_prompt.md` (전처리·시스템프롬프트 수정)
  - `작업지침.md` (스코프 가드레일 추가)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 작업지침.md 7번 일정표 7/23 항목부터, tags.json + narrative_cards.json + prompts/ 3종 + before_after_draft.md를 컨텍스트로 제공
  2. 개발 중 narrative_cards.json의 usage_constraints가 실제로 생성 결과에 잘 반영되는지 확인

---

## 세션 17 — 개발 착수 전 최종 점검 (2026-07-22)

- **목표**: 외부 검토로 발견된 4가지 실제 오류/누락 수정 (랄프 루프 세팅 직전 마지막 점검)
- **완료한 작업**:
  1. **서사 카드 선택 프롬프트 누락 수정**: `prompts/00_narrative_card_selection_prompt.md` 신규 작성 — 3-1-1에서 문서로만 설명하고 실제 프롬프트 파일을 안 만들었던 것을 발견, JSON 출력 스키마(selected_cards/similarity_reasons/abstract_pattern/constraints) 포함해 작성
  2. **프롬프트 2 출력량 축소**: 5단계×2~4장면(총 10~20개)이 3분 데모·before_after_draft.md(5개 장면)와 규모가 안 맞았음 — "5단계별 핵심 장면 1개, 각 2~4문단"으로 수정해 데모 초안과 정합
  3. **태그 진단 근거의 자기모순 수정**: 메타인지/이중시선 태그의 예상 근거 "계산적 태도가 예상됨"이 프롬프트 자신의 규칙("입력에 없는 내용을 근거로 만들지 말 것")을 위반하고 있었음 — "평생을 산 성인의 기억을 유지한 채 갓 태어난 아이가 됨"으로 관찰 가능한 사실만 남기고, "계산적 태도"는 3-2의 톤조건으로 이미 올바르게 분리되어 있음을 확인. prompts/01과 상세기획문서.md 6번 데모 테이블 양쪽 다 수정. 프롬프트1에 "성격·태도 추론 금지, 톤조건은 3-2가 담당" 규칙 명시적으로 추가
  4. **작업지침.md 오염 문구 제거**: 스코프 가드레일의 "실시간 채용 연동"이 이전 부산 청년 챗봇 기획(다른 프로젝트)에서 복사돼 남아있던 흔적임을 발견 — StoryLens 실제 out-of-scope(4번) 항목으로 교체
  5. 전체 파일 대상으로 다른 프로젝트 관련 오염 문구 재검색 — 추가 오염 없음 확인
- **발견한 이슈/블로커**: 없음 (전부 외부 검토가 짚어준 실제 결함이었고, 개발 착수 전에 잡아서 랄프 루프에 잘못된 컨텍스트가 들어가는 것을 방지함)
- **변경된 파일**:
  - `prompts/00_narrative_card_selection_prompt.md` (신규)
  - `prompts/01_tag_diagnosis_prompt.md` (근거 규칙 강화, 예상출력 수정)
  - `prompts/02_structure_generation_prompt.md` (출력 형식 축소)
  - `상세기획문서.md` (6번 데모 테이블 수정)
  - `작업지침.md` (스코프 가드레일 오염 제거)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 작업지침.md 7번 일정표 7/23 항목부터, tags.json + narrative_cards.json + prompts/ 4종(00~03) + before_after_draft.md를 컨텍스트로 제공
  2. 개발 시작 전 최종 파일 일관성 재확인 (present_files로 전체 스냅샷 공유 권장)

---

## 세션 18 — 실제 배치 경로 확정 및 다운로드 기능 추가 (2026-07-22)

- **목표**: 5번째 지적(파일 경로 분리) 반영 + 결과물 다운로드 기능 스코프 추가
- **완료한 작업**:
  - `resources/data/tags.json`, `resources/data/narrative_cards.json` — 기존 순수 JSON을 실제 배치 경로로 복사
  - `resources/prompts/card-selection.txt`, `tag-diagnosis.txt`, `structure-generation.txt`, `actor-evaluator.txt` — 각 프롬프트 .md에서 설명·예시를 제거하고 순수 시스템 프롬프트 텍스트만 추출 (`{{placeholder}}` 형태 유지, Spring AI PromptTemplate 또는 문자열 치환으로 런타임 주입)
  - 작업지침.md 6번 파일구조를 실제 배치 경로 기준으로 전면 갱신 — `.md` 문서(설명용)와 실제 로드 파일(`resources/` 하위)을 명확히 구분
  - **다운로드 기능 추가**: 서버 저장 없이 클라이언트 사이드(JS Blob + 다운로드 링크)로 생성 결과를 `.txt`/`.md` 파일로 즉시 저장하는 기능을 4번 필수 범위·7번 기술스택에 추가
  - "다운로드"(클라이언트 사이드, 필수)와 "저장·공유 기능"(서버 영속 저장, 제외 유지)을 명확히 구분하는 문구를 4번, 작업지침.md 스코프 가드레일에 추가 — 랄프 루프가 혼동하지 않도록
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `resources/data/tags.json`, `resources/data/narrative_cards.json` (신규 — 실배치 경로)
  - `resources/prompts/*.txt` 4개 (신규 — 실배치 경로, 순수 프롬프트)
  - `상세기획문서.md` (4, 7번 섹션 수정)
  - `작업지침.md` (6번 파일구조 전면 갱신, 스코프 가드레일 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — `resources/` 폴더를 그대로 `src/main/resources/`에 복사하는 것으로 스캐폴딩 시작
  2. 다운로드 기능의 실제 UI 위치(Before/After 비교 화면 하단 등) 결정은 개발 중 자율 진행

---

## 세션 19 — 10번 섹션 중복 버그 수정 (2026-07-22)

- **목표**: 사용자가 지적한 "태그 우선순위/가중치 로직" 항목이 계속 미체크로 보이는 문제 확인
- **완료한 작업**:
  - 원인 파악: 세션12에서 "기술 스택 확정" 근처에 체크 완료 줄을 새로 추가할 때, 원래 더 위쪽에 있던 미체크 원본 줄("- [ ] 태그 동시 감지 시 우선순위/가중치 로직 확정")을 함께 지우지 않아 중복으로 남아있었음
  - 중복된 미체크 줄 제거, 체크 완료된 줄에 3-2(기능유형/자유창작영역) 참조까지 보강해 하나로 통합
  - 문서 전체에서 다른 미체크(`- [ ]`) 항목 재검색 — 나머지는 전부 4번(MVP 구현 체크리스트)에 정당하게 남아있는 개발 TODO였고, 10번(결정 필요 사항)의 중복만 버그였음을 확인
- **발견한 이슈/블로커**: 없음 (문서 편집 히스토리에서 생긴 잔재였고, 사용자가 직접 발견해줌)
- **변경된 파일**:
  - `상세기획문서.md` (10번 섹션 중복 제거)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — `resources/` 폴더를 `src/main/resources/`로 복사하며 스캐폴딩 시작

---

## 세션 20 — LLM 프로바이더 확정 (2026-07-22)

- **목표**: 구글 API 무료 티어 연결 방식 확정, 의존성 사전 추가 여부 결정
- **완료한 작업**:
  - Spring AI의 구글 연동 두 방식(전용 Google GenAI 스타터 vs OpenAI 호환 스타터+Gemini 엔드포인트) 조사 — 전용 스타터는 `project-id`/`location` 설정 시 Vertex AI(유료) 모드로 조용히 전환되는 함정 발견
  - **OpenAI 호환 스타터(`spring-ai-starter-model-openai`) + Gemini 엔드포인트 방식 채택** — Vertex 함정 회피, 향후 다른 OpenAI 호환 프로바이더로도 설정만으로 교체 가능
  - 의존성 사전 추가 여부: **불필요, 실제 교체 시점에 추가**로 결론 — Spring AI의 ChatClient 추상화가 이미 교체 비용을 낮춰주므로 안 쓰는 스타터를 미리 넣을 이유 없음. `spring-ai-bom`으로 버전 관리할 것
  - 상세기획문서.md 7번, 작업지침.md 스코프 가드레일 갱신
  - "포니테일 훅" 관련 질문은 다른 프로젝트 소속이라 프로젝트 범위 제한으로 검색 불가 — 사용자에게 설명 요청
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (7번 섹션 수정)
  - `작업지침.md` (스코프 가드레일 추가)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 7/23 프로젝트 스캐폴딩부터 (Spring Initializr + spring-ai-starter-model-openai + Gemini API 키 설정)
  2. "포니테일 훅" 관련 후속 논의 (사용자 설명 대기)

---

## 세션 21 — .gitignore 추가 (2026-07-22)

- **목표**: 저장소 초기화 전 .gitignore 준비
- **완료한 작업**:
  - `.gitignore` 작성 — API 키/시크릿(.env, application-local.yml 등) 최상단 배치, Maven/Gradle 빌드 산출물, Node/React 프론트, IDE, OS 파일 커버
  - 작업지침.md 스코프 가드레일에 "API 키 하드코딩 금지, .gitignore로 분리" 원칙 추가
  - `storylens_project_seed.zip` 재압축 (.gitignore 포함)
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `.gitignore` (신규)
  - `작업지침.md` (스코프 가드레일 추가)
  - `storylens_project_seed.zip` (재압축)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — git init 후 zip 압축 해제, 7/23 프로젝트 스캐폴딩부터

---

## 세션 22 — Git 브랜치/태그 전략 확정 (2026-07-22)

- **목표**: 랄프 루프 세션의 git 워크플로우(브랜치 전환 vs 단일 브랜치) 결정
- **완료한 작업**:
  - 세션별 브랜치 전환과 단일 브랜치 지속 커밋 두 옵션 비교
  - **단일 브랜치 + 세션 완료(DoD 통과) 시마다 git 태그(`session-N`)** 방식으로 확정 — 세션이 순차 종속 구조라 브랜치 분기 이점이 없고, 태그만으로 diff 확인·롤백 체크포인트 기능을 충분히 대체 가능
  - 작업지침.md 3번(코딩 컨벤션)에 반영
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `작업지침.md` (3번 섹션 수정, 오타 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — git init 후 `session-N` 태그 규칙 적용 시작 (7/23 프로젝트 스캐폴딩 = session-14)

---

## 세션 23 — 빌드 도구(Maven) 확정 (2026-07-22)

- **목표**: Maven vs Gradle 결정, .gitignore 패턴 검증
- **완료한 작업**:
  - `.gitignore`의 `reference/**` 패턴 검증 — 루트 기준 앵커링되어 올바르게 동작함 확인 (`reference/`와 동일 효과)
  - Maven vs Gradle 비교 — Spring AI 공식 문서/BOM 예시가 Maven 위주라는 점, 선언적 XML의 낮은 문법 오류 리스크를 근거로 **Maven 확정**
  - 상세기획문서.md 7번, 작업지침.md 3번·6번(파일구조에 pom.xml 추가), .gitignore(Maven 확정 주석) 갱신
- **발견한 이슈/블로커**: 없음
- **변경된 파일**:
  - `상세기획문서.md` (7번 섹션 수정)
  - `작업지침.md` (3, 6번 섹션 수정)
  - `.gitignore` (주석 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — Maven 기반 Spring Initializr로 7/23 스캐폴딩 시작 (session-14 태그부터)

---

## 세션 24 — 향후 아이디어 메모 생성 (2026-07-22)

- **목표**: 대화 중 나왔으나 MVP 범위 밖으로 보류한 아이디어 2건을 잊지 않도록 별도 파일로 정리
- **완료한 작업**:
  - `향후_아이디어_메모.md` 신규 생성
  - 아이디어 1: 플롯 궤적 변주 (부커 7유형 대신 회귀물 팬덤의 "회귀의 도덕적 대가" 논쟁을 씨앗으로)
  - 아이디어 2: 임베딩 기반 문체 검증/few-shot 확장 — 검증 용도는 순환논리 위험으로 보류, few-shot 재료 확장 용도는 유망하다고 정리
- **발견한 이슈/블로커**: 없음 (실제 설계 변경 아님, 순수 아이디어 보관)
- **변경된 파일**:
  - `향후_아이디어_메모.md` (신규)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 7/23 스캐폴딩부터 (이 메모 파일은 개발에 직접 관여하지 않음, MVP 제출 후 재검토용)

---

## 세션 25 — 작업지침 파일구조 문서 오류 수정 (2026-07-22)

- **목표**: 사용자가 발견한 실제 zip 구조와 작업지침.md 6번 명세 간 불일치 해소
- **완료한 작업**:
  - 실제 `storylens_project_seed` 구조와 작업지침.md 6번 트리 비교
  - 진짜 누락 발견: `reference/` 폴더, `향후_아이디어_메모.md`, `.gitignore`가 트리에서 빠져있었음 — 추가
  - 의도된 차이 명확화: zip의 `resources/`는 스캐폴딩 전 "재료" 상태이고, 트리의 `src/main/resources/`는 "스캐폴딩 완료 후 최종 상태"임을 명시하는 안내 문구 추가 — 두 시점이 다르다는 걸 문서에 드러내지 않아 혼동 발생했음
- **발견한 이슈/블로커**: 없음 (문서 갱신 누락이었고, 실제 파일 내용에는 문제 없었음)
- **변경된 파일**:
  - `작업지침.md` (6번 섹션 수정)
  - `로그.md` (본 세션 기록 추가)
- **다음 세션 TODO**:
  1. 랄프 루프 개발 착수 — 7/23 스캐폴딩부터, zip의 `resources/`를 `src/main/resources/`로 이동하는 것으로 시작

---

## 세션 26 — 프로젝트 스캐폴딩 (2026-07-23)

- **목표**: Maven 기반 Spring Boot/Spring AI와 React(Vite)/Tailwind CSS 프로젝트 초기화
- **완료한 작업**:
  - Spring Boot 4.1.0 + Spring AI 2.0.0 Maven 프로젝트와 애플리케이션 진입점 생성
  - `spring-ai-bom`으로 `spring-ai-starter-model-openai` 버전 관리
  - `resources/data/`, `resources/prompts/`의 6개 파일을 `src/main/resources/`로 이동하고 blob 해시 일치 확인
  - React(Vite) 프로젝트 생성 및 Tailwind CSS Vite 플러그인 연결
  - Gemini OpenAI 호환 엔드포인트와 `GEMINI_API_KEY` 환경변수 설정 틀 작성
  - `mvn compile`, `npm run build`, `npm run lint`, `npm run dev` 및 HTTP 응답 확인
- **발견한 이슈/블로커**:
  - 블로커 없음
  - 검증 환경에 JDK/Maven이 없어 `/tmp`에 임시 도구를 내려받아 컴파일했으며 저장소에는 포함하지 않음
  - 샌드박스 포트 제한으로 Vite 최초 기동이 차단됐으나 권한 승인 후 정상 기동 확인
- **변경된 파일**:
  - `pom.xml`
  - `src/main/java/com/storylens/StoryLensApplication.java`
  - `src/main/resources/application.yml`
  - `src/main/resources/data/`
  - `src/main/resources/prompts/`
  - `frontend/`
  - `로그.md`
- **다음 세션 TODO**:
  1. 7/24 일정의 입력 → 태그 진단 API 구현

> 참고: 이 세션의 git 태그는 실수로 `session-14`로 찍혔음 (Codex가 예전 세션 번호를 재사용). 실제로는 세션 26. 이후 세션부터는 로그 번호와 태그 번호를 일치시킴.

---

## 세션 27 — 태그 진단 API 구현 (2026-07-23)

- **목표**: 7/24 일정의 입력 → 태그 진단(3-1) API 연결 완료
- **완료한 작업**:
  - `tags.json`을 앱 시작 시 로드하고 9개 태그 id를 메모리에 보관하는 컴포넌트 구현
  - `tag-diagnosis.txt`를 로드해 `{{tag_definitions}}`에 태그 정의를 주입하는 컴포넌트 구현
  - Spring AI `ChatClient` 호출, 응답 JSON 파싱 및 9개 태그 스키마 검증 서비스 구현
  - `POST /api/diagnose` 엔드포인트와 빈 입력 400 처리 구현
  - 유효하지 않은 JSON 응답을 최대 3회까지만 호출하는 재시도 로직 및 단위 테스트 구현
  - `mvn compile`, `mvn test`, 애플리케이션 기동 및 빈 입력 400 응답 확인
  - 로컬 OpenAI 호환 스텁으로 첫 응답 invalid JSON → 재호출 → 9개 태그/5개 활성 결과 반환 종단 테스트 확인
  - 상세기획문서 6번 고정 입력으로 실제 Gemini 연동 `POST /api/diagnose` 호출: HTTP 200, 9개 태그 전체 및 활성 태그 5개 확인
- **발견한 이슈/블로커**:
  - 초기에는 `GEMINI_API_KEY` 부재로 실제 호출이 막혔으나, `.env`에서 키를 로드한 뒤 실제 Gemini 응답(9개 태그/활성 5개)을 확인해 해결
- **변경된 파일**:
  - `pom.xml`
  - `src/main/java/com/storylens/tag/TagDefinitionStore.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisPrompt.java`
  - `src/main/java/com/storylens/tag/DiagnosisResponse.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisService.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisController.java`
  - `src/test/java/com/storylens/tag/TagDiagnosisServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 태그 진단 결과를 사용하는 구조 고정 생성 로직 구현

---

## 세션 28 — 참조 서사 카드 선정 API 구현 (2026-07-23)

- **목표**: 상세기획문서 3-1-1의 태그 기반 후보 필터링 + LLM 구조 비교 구현
- **완료한 작업**:
  - `narrative_cards.json`의 12개 카드를 앱 시작 시 로드하고 id/`connected_tags`를 검증하는 컴포넌트 구현
  - 활성 태그와 하나 이상 겹치는 카드만 추출하고 `검토_후_제외` 카드를 배제한 뒤, 겹침 수 내림차순 상위 4개로 제한하는 후보 필터 구현
  - `card-selection.txt`의 `{{user_input}}`/`{{candidate_cards}}`를 실제 값으로 치환해 Spring AI `ChatClient`로 호출하는 서비스 구현
  - `selected_cards`, `similarity_reasons`, `abstract_pattern`, `constraints` 응답 JSON 파싱·검증과 최대 3회 재시도 구현
  - 태그 진단을 먼저 실행하는 임시 테스트 엔드포인트 `POST /api/select-card`와 빈 입력 400 처리 구현
  - 후보 정렬·4개 제한 및 3회 재시도 단위 테스트 추가
  - `mvn compile`, `mvn test` 성공 (테스트 3개 통과)
  - 상세기획문서 6번 고정 입력으로 실제 Gemini 연동 호출: HTTP 200, `selected_cards`에 `count_of_monte_cristo` 포함 확인
  - `abstract_pattern`에 원작 고유 인물명 `몬테크리스토`/`당테스`가 포함되지 않음을 확인
- **발견한 이슈/블로커**:
  - 블로커 없음
  - 기본 Maven 캐시 `/home/flatbass/.m2`가 샌드박스에서 읽기 전용이라 `/tmp/storylens-m2`를 사용해 검증함
- **변경된 파일**:
  - `src/main/java/com/storylens/cardselection/NarrativeCardStore.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionResponse.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionService.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionController.java`
  - `src/test/java/com/storylens/cardselection/CardSelectionServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 구조 고정 생성 로직을 구현하고 임시 `POST /api/select-card`를 통합 엔드포인트로 합치기

---

## 세션 29 — 구조 고정 생성 API 구현 (2026-07-24)

- **목표**: 상세기획문서 3-2의 단계 배치·기능유형 고정 조건을 반영한 구조 생성 통합 API 구현
- **완료한 작업**:
  - `tags.json`의 `master_plot_stage`를 이용해 활성 태그를 상실/회귀_각성/목표_설정/실행/응징_도달 5단계에 모두 배치하는 컴포넌트 구현
  - `typical_functional_type`으로 활성 태그를 핵심목표/필수사건/지속조건/톤조건에 그룹핑하고, 태그별 `psychological_function`과 진단 `evidence`를 한국어 고정 조건 문장으로 합치는 로직 구현
  - 상세기획문서 5-2의 오리지널 few-shot 원문을 별도 리소스로 저장하고 구조 생성 프롬프트에 주입
  - 사용자 원본 설정, 카드 선정의 `abstract_pattern`/`constraints`, 4개 고정 조건, 5단계 태그, few-shot을 `structure-generation.txt`의 모든 자리표시자에 채워 Spring AI `ChatClient`로 호출
  - 정확히 5개 단계와 단계별 2~4문단을 검증하는 JSON 응답 파싱 및 최대 3회 재시도 구현
  - 태그 진단 → 참조 카드 선정 → 구조 생성 순서의 `POST /api/generate` 구현 및 임시 `POST /api/select-card` 제거
  - `mvn test` 성공 (테스트 5개 통과)
  - 상세기획문서 6번 고정 입력으로 실제 Gemini 연동 호출: HTTP 200, 순서가 고정된 5개 장면과 각 3문단 반환 확인
  - 생성 결과에 `몬테크리스토`/`당테스`가 없고, 목표 설정 단계의 후계자 등극 의지와 응징·도달 단계의 후계자 등극·배신자 응징 반영을 육안 확인
- **발견한 이슈/블로커**:
  - 블로커 없음
  - 실제 호출에서 카드 선정 응답이 마크다운 코드 블록으로 감싸져 2회 파싱 실패했으나, 기존 최대 3회 재시도로 3번째 호출에 정상 처리됨
  - 생성 결과에 일부 단어 수준의 오탈자가 있었으나 5단계 구조와 고정 조건은 충족함
- **변경된 파일**:
  - `src/main/java/com/storylens/tag/TagDefinitionStore.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionService.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionController.java` (제거)
  - `src/main/java/com/storylens/plot/PlotStructureMapper.java`
  - `src/main/java/com/storylens/plot/GenerationResponse.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/java/com/storylens/plot/StructureGenerationController.java`
  - `src/main/resources/prompts/structure-generation.txt`
  - `src/main/resources/prompts/style-few-shot-example.txt`
  - `src/test/java/com/storylens/cardselection/CardSelectionServiceTest.java`
  - `src/test/java/com/storylens/plot/PlotStructureMapperTest.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 상세기획문서 3-3의 체크리스트 기반 Actor-Evaluator 자체 검증과 FAIL 교정 재생성 구현

---

## 세션 30 — Actor-Evaluator 자체 검증 구현 (블로커) (2026-07-24)

- **목표**: 상세기획문서 3-3의 7개 체크리스트 검증과 FAIL 시 최대 2회 교정 재생성 구현
- **완료한 작업**:
  - `actor-evaluator.txt`의 4개 고정 조건과 생성 아웃라인 자리표시자를 채워 Spring AI `ChatClient`로 호출하는 검증 서비스 구현
  - `overall_pass_fail`, 7개 `checklist`, `violated_constraint`, `correction_instruction` JSON 파싱·스키마 검증 및 최대 3회 파싱 재시도 구현
  - FAIL이면 `correction_instruction`을 구조 생성 프롬프트에 추가해 재생성·재검증하고, 최대 2회 후 마지막 결과를 반환하는 종료 보장 루프 구현
  - 기존 `scenes`를 유지하면서 `verification`과 `regeneration_count`를 추가한 `POST /api/generate` 응답 스키마 구현
  - 검증 JSON 3회 파싱 재시도와 FAIL 재생성 2회 상한을 검증하는 테스트 추가
  - `mvn test` 성공 (테스트 7개 통과, 컴파일 포함)
  - 고정 데모 입력의 실제 응답을 `/tmp/storylens-generate-with-verification.json`에 저장
- **발견한 이슈/블로커**:
  - **블로커**: 실제 Gemini 호출에서 세션 29부터 관찰된 서사 카드 선정 응답의 마크다운 코드 블록 감싸기가 3회 연속 반복되어 `CardSelectionService` JSON 파싱이 모두 실패하고 HTTP 502로 종료됨
  - 저장된 `/tmp/storylens-generate-with-verification.json`은 HTTP 502 오류 응답이며, 파이프라인이 Actor-Evaluator 단계에 도달하지 못해 실제 `verification.checklist` 7개를 확인하지 못함
  - 같은 오류 3회 반복 시 중단한다는 작업지침에 따라 추가 호출이나 코드 블록 제거 우회는 시도하지 않음
- **변경된 파일**:
  - `src/main/java/com/storylens/plot/GenerationResponse.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/java/com/storylens/verify/VerificationResponse.java`
  - `src/main/java/com/storylens/verify/ActorEvaluatorService.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `src/test/java/com/storylens/verify/ActorEvaluatorServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 사람 판단 후 모든 LLM JSON 응답에서 마크다운 코드 블록을 정규화할지, Gemini 응답 형식을 별도 설정으로 강제할지 결정
  2. 결정된 방식 적용 후 고정 입력의 `/api/generate` 응답을 다시 파일로 저장하고 `checklist=7`, `regeneration_count=0~2`를 확인

---

## 세션 31 — LLM JSON 응답 안정화 (쿼터 블로커) (2026-07-24)

- **목표**: 4개 LLM 서비스의 순수 JSON 응답 강제와 마크다운 코드펜스 방어 처리
- **완료한 작업**:
  - Spring AI 2.0.0의 `OpenAiChatOptions`에 `response_format=JSON_OBJECT`를 지정하는 공용 옵션 구현
  - 응답 앞뒤의 마크다운 JSON 코드펜스를 제거하는 `JsonResponseSupport.clean` 구현
  - `TagDiagnosisService`, `CardSelectionService`, `StructureGenerationService`, `ActorEvaluatorService`의 모든 LLM 호출과 JSON 파싱에 공용 옵션·정리 로직 적용
  - 4개 서비스 각각의 코드펜스 응답 정상 파싱 테스트와 `JSON_OBJECT` 옵션 테스트 추가
  - 기존 최대 3회 JSON 파싱 재시도 로직 유지
  - 평가 LLM 원문에는 없는 `regeneration_count`를 원시형으로 역직렬화하던 문제를 nullable `Integer`로 수정하고, 최종 API 응답 조립 시 0~2 값을 넣도록 유지
  - `mvn compile`, `mvn test` 성공 (테스트 11개 통과)
  - 고정 데모 실제 1차 호출 성공: `/tmp/storylens-generate-with-verification.json`에 HTTP 200 응답 저장, `scenes=5`, `verification.checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0` 확인
- **발견한 이슈/블로커**:
  - 세션 30의 마크다운 코드펜스 파싱 오류는 단위 테스트와 실제 1차 호출에서 재발하지 않아 해결 확인
  - **블로커**: 이후 호출에서 Gemini `gemini-3.6-flash` 무료 티어 일일 요청 한도 20회를 초과한 `429 RESOURCE_EXHAUSTED`가 3회 연속 발생
  - `/tmp/storylens-generate-with-verification-2.json`, `/tmp/storylens-generate-with-verification-3.json`에는 HTTP 500 오류 응답이 저장됨
  - 동일 429가 3회 반복되어 작업지침에 따라 추가 호출을 중단했으며, 3회 연속 정상 응답 검증은 쿼터 갱신 후 재실행 필요
- **변경된 파일**:
  - `src/main/java/com/storylens/ai/JsonResponseSupport.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisService.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionService.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/java/com/storylens/verify/ActorEvaluatorService.java`
  - `src/main/java/com/storylens/verify/VerificationResponse.java`
  - `src/test/java/com/storylens/ai/JsonResponseSupportTest.java`
  - `src/test/java/com/storylens/tag/TagDiagnosisServiceTest.java`
  - `src/test/java/com/storylens/cardselection/CardSelectionServiceTest.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `src/test/java/com/storylens/verify/ActorEvaluatorServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. Gemini 무료 티어 쿼터 갱신 후 같은 고정 입력으로 `/api/generate` 3회 연속 HTTP 200 및 각 응답의 `checklist=7`, `regeneration_count=0~2`를 확인

---

## 세션 32 — OpenAI 전환 및 3회 연속 검증 완료 (2026-07-24)

- **목표**: Gemini 429 블로커를 OpenAI 전환으로 해소하고 서비스별 모델 배정과 고정 데모 3회 연속 검증 완료
- **완료한 작업**:
  - `application.yml`의 API 키를 `${LLM_API_KEY}`로 변경하고 `base-url`을 `https://api.openai.com/v1`로 전환
  - 전역 모델 설정을 제거하고 각 서비스 호출 옵션에 모델을 개별 지정:
    - `TagDiagnosisService`: `gpt-5-nano`
    - `CardSelectionService`: `gpt-5-mini`
    - `StructureGenerationService`: `gpt-5.6-luna`
    - `ActorEvaluatorService`: `gpt-5.6-terra`
  - `JsonResponseSupport.options(model)`이 서비스별 모델과 기존 `JSON_OBJECT` 응답 형식을 함께 적용하도록 변경
  - 서비스별 테스트에서 실제 전달된 모델명을 검증하고 공용 옵션 테스트에 모델 검증 추가
  - OpenAI `/v1/models` 호출 HTTP 200 및 요청한 4개 모델의 계정 제공 여부 확인
  - `gpt-5-nano`가 활성 태그 6개만 반환한 초기 실패를 확인하고, 비활성 태그를 포함한 9개 전체 반환을 `tag-diagnosis.txt`에 명시
  - 프롬프트 수정 후 `/api/diagnose` HTTP 200 및 `tag_results=9` 확인
  - 최종 `mvn test` 성공 (테스트 11개 통과, 컴파일 포함)
  - 같은 고정 데모 입력으로 `/api/generate` 3회 연속 HTTP 200 확인:
    - `/tmp/storylens-openai-generate-1.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
    - `/tmp/storylens-openai-generate-2.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
    - `/tmp/storylens-openai-generate-3.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
- **발견한 이슈/블로커**:
  - 최초 OpenAI 통합 호출은 `gpt-5-nano`가 비활성 태그를 생략해 HTTP 502였으나 프롬프트 명시 강화로 해결
  - 현재 블로커 없음
- **변경된 파일**:
  - `src/main/resources/application.yml`
  - `src/main/resources/prompts/tag-diagnosis.txt`
  - `src/main/java/com/storylens/ai/JsonResponseSupport.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisService.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionService.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/java/com/storylens/verify/ActorEvaluatorService.java`
  - `src/test/java/com/storylens/ai/JsonResponseSupportTest.java`
  - `src/test/java/com/storylens/tag/TagDiagnosisServiceTest.java`
  - `src/test/java/com/storylens/cardselection/CardSelectionServiceTest.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `src/test/java/com/storylens/verify/ActorEvaluatorServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 없음 — OpenAI 전환과 Actor-Evaluator 실제 연속 검증 완료

---

## 세션 33 — LLM 프로바이더 전환 결정 (Gemini → OpenAI, 서비스별 모델 배정) (2026-07-24)

> 참고: 이 세션은 실제로는 세션32(위)의 Codex 작업 착수 **이전**에, 대화 중 결정된 내용을 기록한 것임. 번호가 실제 시간 순서와 반대로 보일 수 있으나, 세션32 작업 결과를 정리하는 과정에서 원래 있던 "세션32" 번호가 이 결정 논의와 겹쳐 33번으로 재배치함 (2026-07-24 로그 정합성 점검 때 발견).

- **목표**: 세션31에서 발생한 Gemini 무료 티어 429 쿼터 블로커 해결 방침 확정
- **완료한 작업**:
  - 원인 분석: Gemini 3.6 Flash 무료 티어 한도(RPM 5, RPD 20)가 낮고, `/api/generate` 1회 호출당 내부적으로 LLM 4단계(진단→카드선정→구조생성→검증)가 연쇄 호출되어 테스트 몇 번만으로도 한도 소진됨을 확인
  - 옵션 비교: (A) Gemini 결제 연결 — Vertex AI 모드 전환 없이 순수 api-key 방식 유지하며 한도만 상향 가능함을 문서로 확인, (B) 결제 없이 구형 모델로 다운그레이드, (C) 이미 결제 활성화된 OpenAI API로 전환
  - **OpenAI(C)로 전환 확정** — 이미 결제된 계정이 있어 즉시 진행 가능
  - 모델 배정 표를 참고해 **서비스별로 다른 모델 개별 배정** 결정: TagDiagnosisService=gpt-5-nano(추출), CardSelectionService=gpt-5-mini(매칭), StructureGenerationService=gpt-5.6-luna(본문생성), ActorEvaluatorService=gpt-5.6-terra(검증, 호출빈도 낮아 상위모델 투입)
  - 환경변수명 GEMINI_API_KEY → LLM_API_KEY로 프로바이더 중립화 결정
  - 이 배정 자체를 8번 심사기준 "AI 활용성"의 오케스트레이션 근거로 채택
  - 상세기획문서.md 7번, 8번 섹션 갱신, 작업지침.md 스코프 가드레일 갱신
- **발견한 이슈/블로커**: 없음 (이 결정을 실제로 구현·검증한 결과는 위 세션32에 반영됨)
- **변경된 파일**:
  - `상세기획문서.md` (7, 8번 섹션 수정)
  - `작업지침.md` (LLM 연동 가드레일 수정)
  - `로그.md` (본 세션 기록 추가, 이후 세션32/33 번호 정합성 점검으로 재수정)
- **다음 세션 TODO**:
  1. 없음 — 세션32에서 이미 실행·검증 완료

---

## 세션 34 — 프론트엔드 기능 골격 및 백엔드 연동 (2026-07-24)

- **목표**: 단일 `/api/generate` 호출로 진단·생성·검증 결과를 받아 입력→진단→결과 3화면과 다운로드 기능 구현
- **완료한 작업**:
  - `GenerationResponse`에 기존 `scenes`, `verification`을 유지하면서 `diagnosis` 필드 추가
  - `StructureGenerationService`가 이미 생성한 `DiagnosisResponse`를 최종 응답에 포함하도록 변경하고 단위 테스트에 동일 진단 결과 반환 검증 추가
  - React 단일 컴포넌트의 상태 전환으로 입력, 진단 결과, 최종 결과 3화면 구현
  - 입력 제출 중 로딩 상태와 180초 타임아웃, API 실패 메시지 구현
  - 진단 화면에 9개 태그명, `display_strength`, `evidence` 표시
  - 결과 화면에 순서가 고정된 5개 장면과 `overall_pass_fail`, 체크리스트 7개 표시
  - 브라우저 기본 `Blob`과 `<a download>`를 사용한 `storylens-outline.txt` 다운로드 구현
  - Vite 개발 프록시로 `/api`를 `http://localhost:8080`에 연결해 Spring Boot CORS 설정 추가 없이 로컬 연동
  - `mvn compile`, `mvn test` 성공 (테스트 11개 통과)
  - `npm run build`, `npm run lint` 성공
  - 백엔드와 프론트 개발 서버 동시 실행 후 상세기획문서 6번 고정 입력을 실제 브라우저에서 검증:
    - 입력→진단→결과 화면 전환, 태그 9개, 장면 5개, 체크리스트 7개, `overall_pass_fail=PASS` 확인
    - 브라우저 콘솔 오류·요청 실패·CORS 오류 없음
    - 다운로드된 `/tmp/storylens-outline.txt` 5,599바이트 및 5개 단계명·장면 포함 확인
    - 백엔드 중단 상태에서 `요청에 실패했습니다. (502)` 오류 메시지 표시 확인
- **발견한 이슈/블로커**:
  - 프로젝트 블로커 없음
  - 브라우저 자동 검증 환경에 Chromium 공유 라이브러리가 없어 `/tmp`에만 런타임을 보완했으며 저장소 의존성은 추가하지 않음
- **변경된 파일**:
  - `src/main/java/com/storylens/plot/GenerationResponse.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `frontend/src/App.jsx`
  - `frontend/vite.config.js`
  - `로그.md`
- **다음 세션 TODO**:
  1. RPG 상태창 스타일과 Framer Motion 애니메이션 적용
  2. Before/After 생성 로직 구현 후 비교 UI 추가

---

## 세션 35 — Before 생성 및 비교 UI 구현 (2026-07-24)

- **목표**: 동일 모델·입력·길이 조건의 가드레일 없는 Before 생성과 기존 After의 좌우 비교 화면 구현
- **완료한 작업**:
  - `gpt-5.6-luna`를 사용하는 `NaiveGenerationService`와 단순 `NaiveGenerationResponse` 구현
  - 태그 진단, 참조 카드, 고정 조건, few-shot, Actor-Evaluator 없이 사용자 원문과 5개 장면·2~4문단·JSON 형식만 지시하는 Before 프롬프트 적용
  - `POST /api/generate-naive`를 기존 `/api/generate`와 동일한 입력 스키마로 추가
  - Before 응답의 `장면1`~`장면5`, 장면별 2~4문단 검증과 최대 3회 JSON 재시도 구현
  - `NaiveGenerationServiceTest`에서 5개 장면과 `gpt-5.6-luna` 모델 지정 검증
  - 프론트에서 `/api/generate`와 `/api/generate-naive`를 `Promise.allSettled`로 동시에 호출하도록 변경
  - 진단 화면 이후 왼쪽 Before·오른쪽 After의 2단 비교 레이아웃 구현
  - Before에는 진단·자체 검증 UI를 제외하고, After에만 기존 검증 체크리스트와 다운로드 유지
  - 한쪽 호출 실패 시 실패한 쪽만 오류를 표시하고 성공한 쪽 5개 장면은 유지하는 양방향 브라우저 모의 검증 통과
  - 빈 줄이 없을 때 단일 줄바꿈도 문단 경계로 인정하는 공용 문단 계산을 Before·After에 동일 적용
  - `mvn compile`, `mvn test` 성공 (테스트 13개 통과)
  - `npm run build`, `npm run lint` 성공
  - 상세기획문서 6번 고정 입력으로 실제 브라우저 검증 완료:
    - 두 API 요청 시작 시각 차이 0ms, 모두 HTTP 200
    - 왼쪽 Before 5개 장면, 오른쪽 After 5개 단계, After 체크리스트 7개·`overall_pass_fail=PASS`
    - 좌우 2단 배치, 브라우저 콘솔·요청·CORS 오류 없음
    - `/tmp/storylens-before.json`, `/tmp/storylens-after.json`에 실제 응답 저장
    - Before 1,422자·After 1,556자로 유사 길이 확인
    - Before는 미래 정보가 1회 추상적으로 언급되고 증거 수집·회장 선출로 빠르게 진행된 반면, After는 미래의 분식회계 날짜·계좌 활용과 기억 상실 대가를 반복해 정보우위·지속조건을 구체적으로 유지
    - 비교 화면의 After 다운로드 파일 `/tmp/storylens-after-outline.txt` 3,864바이트와 5개 단계 포함 확인
- **발견한 이슈/블로커**:
  - 첫 실제 호출에서 모델의 단일 줄바꿈 문단을 빈 줄 문단으로만 계산해 After가 HTTP 502였으나, 2~4개 제한을 유지한 공용 문단 경계 계산으로 해결
  - 최종 호출에서 Before가 5문단 장면을 2회 반환했으나 기존 최대 3회 재시도 안에서 정상 응답
  - 현재 블로커 없음
  - **(사후 발견, 대화 중 사용자 검토)**: 실제 저장된 Before/After 텍스트를 사람이 직접 읽어본 결과, After의 응징·도달 장면에서 "비서실 차장이 전생(태성)의 살해를 자백"하는 서술이 발견됨 — 전생과 중생 후는 평행한 별개의 실재이므로 중생 후의 제3자가 전생 사건을 객관적으로 확인·자백할 수 없다는 서사 논리 문제. 이 발견을 반영해 3-2/3-3에 "전생-중생후 평행성" 규칙 추가 (세션36에서 검증 예정)
- **변경된 파일**:
  - `src/main/java/com/storylens/ai/JsonResponseSupport.java`
  - `src/main/java/com/storylens/plot/NaiveGenerationResponse.java`
  - `src/main/java/com/storylens/plot/NaiveGenerationService.java`
  - `src/main/java/com/storylens/plot/StructureGenerationController.java`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/test/java/com/storylens/ai/JsonResponseSupportTest.java`
  - `src/test/java/com/storylens/plot/NaiveGenerationServiceTest.java`
  - `frontend/src/App.jsx`
  - `로그.md`
- **다음 세션 TODO**:
  1. RPG 상태창 스타일과 Framer Motion 애니메이션 적용
  2. (신규) 전생-중생후 평행성 규칙을 구조생성·검증 프롬프트에 반영하고 재검증 (세션36)

---

## 세션 36 — 전생·중생 후 평행성 규칙 재검증 완료 (2026-07-24)

- **목표**: 세션35에서 추가한 전생·중생 후 평행성 규칙의 런타임 반영 여부와 고정 데모 3회 결과 재검증
- **완료한 작업**:
  - `StructureGenerationService`와 `ActorEvaluatorService`가 각각 `prompts/structure-generation.txt`, `prompts/actor-evaluator.txt` 클래스패스 리소스를 직접 로드하며 하드코딩 프롬프트가 없음을 확인
  - 설계 문서용 프롬프트에는 새 규칙이 있으나 실제 런타임 리소스에는 빠져 있던 불일치를 발견하고 두 리소스에 동기화
  - 구조 생성 프롬프트에 전생·중생 후의 평행성, 제3자의 전생 사건 언급·자백 금지, 현재 세계의 새 범죄에 대한 응징 규칙 추가
  - Actor-Evaluator 체크리스트 3번을 (a) 배신·응징 인과, (b) 미래 지식 출처, (c) 제3자의 전생 사건 확인·자백 여부로 확장
  - 두 서비스 테스트에 새 규칙이 실제 LLM 요청 프롬프트에 포함되는지 검증하는 회귀 assertion 추가
  - 구조 생성 출력 지시에 "한 장면이라도 5문단 이상이면 응답 전체가 실패 처리"됨을 명시해 2~4문단 계약을 강화
  - 고정 데모 `/api/generate` 최종 3회 모두 HTTP 200:
    - `/tmp/storylens-parallel-check-1.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
    - `/tmp/storylens-parallel-check-2.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=1`
    - `/tmp/storylens-parallel-check-3.json`: `scenes=5`, `checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
  - 3회 응징·도달 장면 수동 검증 완료:
    - 1회차: 태오만 전생을 발화하고 회장은 이를 확인하지 않으며, 현생의 환자 기록 조작·비자금 세탁·데이터 거래를 처벌
    - 2회차: 이안만 전생의 복수 의미를 가진 대사를 하고, 하린의 질문은 현생의 희생에 관한 것으로 전생 사건 확인이 아님
    - 3회차: 태석은 의심 시점만 질문하고 전생을 언급·확인하지 않으며, 현생의 횡령·약품 조작·살인 교사를 처벌
  - 3회 모두 체크리스트 3번 항목명이 (a) 배신·응징 인과, (b) 미래 지식 출처, (c) 제3자의 전생 사건 확인·자백 여부를 실제로 포함
  - 3회 모두 체크리스트 3번 `evidence`가 각 생성물의 실제 문장과 일치하고, 제3자의 확인·자백이 없다는 판단도 타당함
  - 전체 `mvn test` 성공: 테스트 13개, 실패 0, 오류 0
- **발견한 이슈/블로커**:
  - 최초 실행 묶음의 2회차에서 구조 생성 3회가 모두 5문단을 반환해 HTTP 502가 발생했고, 지침에 따라 즉시 중단·기록
  - 다음 실행에서 출력 지시를 최소 보강한 뒤 3회 전체를 처음부터 재실행해 모두 성공
  - 최종 2회차는 첫 생성이 5문단이라 1회 재생성했으나 두 번째 생성이 통과해 응답 성공
  - 현재 블로커 없음
- **변경된 파일**:
  - `prompts/02_structure_generation_prompt.md`
  - `src/main/resources/prompts/structure-generation.txt`
  - `src/main/resources/prompts/actor-evaluator.txt`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `src/test/java/com/storylens/verify/ActorEvaluatorServiceTest.java`
  - `로그.md`
- **다음 세션 TODO**:
  1. 없음 — 전생·중생 후 평행성 규칙의 런타임 반영과 3회 재검증 완료

---

## 세션 37 — 3개 화면 시각 디자인 및 애니메이션 완성 (2026-07-24)

- **목표**: 입력·진단 결과·Before/After 비교 화면의 RPG 상태창 스타일과 전환 애니메이션 완성
- **완료한 작업**:
  - `framer-motion` 12.42.2 설치 및 화면 전환에 `AnimatePresence` 적용
  - 진단 결과를 3×3 RPG 상태창으로 재구성하고 9개 태그에 순차 등장(stagger) 애니메이션 적용
  - 활성 태그는 청록 강조, 비활성 태그는 흐림 처리하고 감지 강도를 숫자 대신 3칸 게이지로 시각화
  - Before는 무채색, After는 청록·금색 강조 톤으로 분리하고 양쪽 5개 장면에 순차 등장 애니메이션 적용
  - After의 Actor-Evaluator 체크리스트 7개 항목에 PASS/FAIL 아이콘과 개별 카드 스타일 적용
  - 입력 화면을 같은 게임 UI 톤으로 정리하고 제출 대기 중 회전 오비트·스캔 라인 로더 추가
  - `prefers-reduced-motion` 및 키보드 포커스 스타일 유지
  - 프런트 렌더링 코드에서 태그 `score`를 참조하지 않으며 원점수 비노출 확인
  - `npm run lint`, `npm run build` 성공
  - 상세기획문서 6번 고정 입력과 세션35·36에서 저장한 실제 API 응답을 브라우저 요청에 연결해 전체 흐름 검증:
    - 로딩→진단→비교 전환 정상
    - 태그 9개(활성 4개), Before 5장면, After 5장면, 체크리스트 7개 확인
    - 데스크톱 좌우 비교 배치 및 PASS/FAIL 아이콘 7개 확인
    - 브라우저 콘솔 오류·페이지 오류·요청 실패 없음
  - 최종 스크린샷 저장:
    - `/tmp/storylens-final-diagnosis.png`
    - `/tmp/storylens-final-comparison.png`
- **발견한 이슈/블로커**:
  - 최초 `npm install`은 전역 npm 캐시가 읽기 전용이라 실패했으나 `/tmp` 캐시 지정으로 해결
  - 최초 브라우저 실행은 일반 Chromium의 crashpad 초기화 오류가 발생했으나 기존 Playwright headless shell로 전환해 해결
  - 현재 환경에 `LLM_API_KEY`가 없어 신규 백엔드 생성 호출은 생략하고, 이전 세션에서 같은 고정 입력으로 저장한 실제 성공 응답을 사용해 프런트 전체 흐름을 검증
  - 현재 프로젝트 블로커 없음
- **변경된 파일**:
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `frontend/src/App.jsx`
  - `frontend/src/index.css`
  - `로그.md`
- **다음 세션 TODO**:
  1. 없음 — 데모 3개 화면의 시각 디자인과 애니메이션 완료

## 세션 38 — 공개 데모 외 3개 태그 조합 QA (2026-07-24)

- **목표**: 상세기획문서 9-1의 내부 QA 3종을 실제 `POST /api/generate`로 호출해 공개 데모 케이스에만 맞춘 하드코딩 여부 검증
- **재현 입력값**:
  - **케이스 A — 회귀 + 복수 + 정보우위**
    > 항만 화재 수사를 맡은 재난조사관 윤서는 동료 민혁이 조작한 보고서 때문에 방화범으로 몰리고, 증거를 찾으려다 창고 폭발로 죽는다. 눈을 뜨자 사고가 나기 30일 전 자신의 몸으로 돌아와 있었다. 윤서는 이전 삶에서 직접 확인한 폭발 시간표와 민혁이 CCTV 기록을 바꾸는 순서를 이미 알고 있다. 이번에는 그 정보를 이용해 희생자들을 구하고 조작 원본을 먼저 확보한 뒤, 자신을 배신한 민혁이 법정에서 그날의 대가를 치르게 하겠다고 결심한다.
  - **케이스 B — 빙의 + 신분위장 + 낯선세계 적응**
    > 현대의 기상 연구원 한별은 폭풍을 관측하다가 지도에도 없는 군도 왕국의 추방된 항해사 세라의 몸에 빙의한다. 이 세계에서는 조류가 노랫소리에 따라 방향을 바꾸고, 본명을 세 번 말하면 바다와 맺은 계약이 수명을 가져간다. 외부 영혼은 처형된다는 법 때문에 한별은 자신이 세라인 척 말투와 항해 습관을 익히며 정체를 숨겨야 한다. 낯선 마법 항법과 섬들의 금기를 하나씩 배워 다가오는 대난파를 막고, 원래 세계로 돌아갈 문을 찾는 것이 목표다.
  - **케이스 C — 메타인지/이중시선(시스템) + 영웅서사·시련과 희생**
    > 도시를 삼킨 균열에 들어간 구조대장 지후의 눈앞에 동료들의 생존 확률과 구조 행동의 손익을 수치로 보여 주는 상태창이 열린다. 그는 겁에 질린 동료들과 함께 싸우는 대장인 동시에, 파티의 스탯과 다음 단계의 보상을 냉정하게 계산하는 플레이어처럼 자신과 전장을 관찰한다. [시스템 알림] 다섯 재난을 모두 돌파해야 균열의 핵을 닫을 수 있으며, 각 시련을 넘을 때마다 능력 하나를 영구히 바쳐야 한다. 지후는 동료들을 살리기 위해 회복 능력과 청력을 차례로 희생하면서도 마지막 시련까지 완수해 도시를 구하려 한다.
- **실제 API 결과**:
  - 세 케이스 모두 HTTP 200, JSON 파싱 성공, 502 없음
  - 결과 파일:
    - `/tmp/storylens-qa-a.json`
    - `/tmp/storylens-qa-b.json`
    - `/tmp/storylens-qa-c.json`
  - **케이스 A**:
    - 활성 태그: `regret_retry`, `revenge_retribution`, `informational_advantage_twist` — 의도 태그 3/3 감지
    - 5단계 순서 정상, 장면별 문단 수 `3/3/3/4/3`
    - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
    - 전생의 폭발·배신은 윤서의 기억으로만 다루고, 중생 후 민혁은 현재 세계에서 새로 저지른 범죄의 증거로 처벌됨. 제3자가 전생 사건을 객관적 사실로 언급하거나 자백하지 않아 전생-중생 후 평행성 규칙 준수
  - **케이스 B**:
    - 활성 태그: `disguise_hidden_identity`, `unfamiliar_world_adaptation`, `identity_ambiguity` — 빙의·신분위장은 결합 태그, 낯선세계 적응은 별도 태그로 의도 조합 전부 감지
    - 5단계 순서 정상, 장면별 문단 수 `2/2/2/2/2`
    - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
    - 추가 감지된 `identity_ambiguity`는 한별/세라 사이의 정체성 불안이 입력에 명시되어 있어 오탐으로 보지 않음
  - **케이스 C**:
    - 활성 태그: `heroic_trial_sacrifice`, `metacognitive_dual_perspective` — 의도 태그 2/2 감지
    - 5단계 순서 정상, 장면별 문단 수 `2/3/3/4/3`
    - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
- **발견된 이슈**:
  - 케이스 C 입력과 생성문의 시스템 조건은 "다섯 재난 각각을 넘을 때 능력 하나를 영구히 희생"하는 것이지만, 생성문은 두 번째 재난인 붕괴 미로에서 희생한 능력을 제시하지 않음
  - 그럼에도 Actor-Evaluator는 체크리스트 2번(필수사건이 실제 행동/사건으로 나타나는가)을 PASS로 판정하고 `overall_pass_fail=PASS`를 반환함. 일부 희생 사례만 근거로 전체 반복 조건을 통과시킨 검증 오탐이며, 다음 세션에서 생성·평가 프롬프트의 반복 조건 검증 강화를 검토할 사안
  - 이번 세션 범위에 따라 코드 수정은 하지 않음
- **완료 판정**:
  - 3개 케이스 모두 시스템 오류 없이 응답
  - 각 케이스의 의도 태그 최소 기준 충족(A 3/3, B 결합 태그 포함 전부, C 2/2)
  - 발견된 서사 조건 누락과 검증 오탐을 기록했으므로 이번 세션 완료 기준 충족
- **변경된 파일**:
  - `로그.md`
- **Git**:
  - `git add`, `git commit` 하지 않음

## 세션 39 — 반복 조건 전체 검증 및 케이스 C 재검증 (2026-07-24)

- **목표**: 상세기획문서 3-3 체크리스트 2번의 반복 패턴 전체 검증 반영 여부를 확인하고, 세션38 케이스 C와 6번 고정 데모 입력을 실제 `POST /api/generate`로 재검증
- **런타임 반영 확인**:
  - `ActorEvaluatorService`는 `prompts/actor-evaluator.txt`를 `ClassPathResource`로 로드하며 체크리스트를 하드코딩하지 않음
  - 테스트에서 실제 평가 요청에 `"그 반복 횟수 전부에서 빠짐없이 나타나는지 확인"` 문구가 포함되는지 검증하도록 보강
  - 점검 중 `structure-generation.txt`에 서비스가 요구하는 `{{user_input}}` 블록과 JSON 출력 스키마가 누락된 상태를 발견해 복구
- **케이스 C 최종 재검증**:
  - 세션38 입력값을 그대로 사용해 HTTP 200 응답을 `/tmp/storylens-repeat-check.json`에 저장
  - 활성 태그: `informational_advantage_twist`, `heroic_trial_sacrifice`, `metacognitive_dual_perspective`
  - 다섯 재난의 영구 희생이 각각 명시됨:
    1. 통각 억제 능력
    2. 회복 능력
    3. 청력
    4. 균형 감각·방향 감각
    5. 목소리
  - 체크리스트 2번은 위 다섯 희생을 모두 열거한 evidence로 정당하게 `PASS`
  - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=1`
  - 생성 자체가 반복 조건을 충족했으므로 3-2에 별도 반복 지침을 추가할 필요는 없다고 판단
- **고정 데모 회귀 검증**:
  - 상세기획문서 6번 입력값으로 HTTP 200 응답을 `/tmp/storylens-fixed-demo-repeat-check.json`에 저장
  - 5단계 순서 정상, 장면별 문단 수 `2/3/2/3/3`
  - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
- **테스트**:
  - `mvn -Dmaven.repo.local=/tmp/storylens-m2 test` 성공
  - 총 13개 테스트, 실패 0, 오류 0
- **오류 및 블로커**:
  - 전역 Maven 저장소가 읽기 전용인 문제는 `/tmp/storylens-m2` 사용으로 해결
  - 누락된 JSON 출력 스키마 때문에 OpenAI가 같은 400 오류를 2회 반환했으나, 스키마 복구 후 다음 호출이 성공해 3회 반복 블로커에는 해당하지 않음
  - 구조 생성의 2~4문단 검증에서 연속 2회 재시도가 발생한 호출이 있었으나 세 번째 시도에 성공
  - 현재 블로커 없음
- **변경된 파일**:
  - `src/main/resources/prompts/structure-generation.txt`
  - `src/test/java/com/storylens/verify/ActorEvaluatorServiceTest.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `로그.md`
- **Git**:
  - `git add`, `git commit` 하지 않음

---

## 세션 40 — 실브라우저 테스트 발견 및 태그 데이터 보강 (2026-07-24)
 
- **목표**: 사용자가 직접 브라우저에서 진행한 고난도 입력(책빙의 소재) 테스트 결과 검토 및 즉시 조치 가능한 항목 반영
- **테스트 입력**
  - **리오넬** :전직 헌터 백지헌,
은퇴 직전, 한때 자신이 읽었던 로맨스 판타지 속 악역 북부 대공 ‘리오넬 루시우스 알베론 바렌하이트’의 몸에서 눈을 뜬다.
눈앞엔 붉게 물든 전장, 그리고 고장난 시스템 메시지 하나.
"악역의 임무를 완수하세요."
그래, 원작처럼 행동하면 되는 거야? 폭정을 일삼다 단두대에서 죽으면 임무 완료? 그는 기계처럼 망가진 루트를 따랐고, 결국 원작과 똑같이 죽었다.
...그런데 다시, 처음?
“대체 나한테 원하는 게 뭐야?”
의미 없는 루프 속, 이대로 끝낼 수 없다고 자각한 ‘리오넬’.
세상은 그를 악역이라 불렀고, 그는 주인공이 될 수 없었다.
그가 가진 건 단 하나, 현대인의 냉철한 판단력과, 무너진 질서를 다시 써버릴 법전 한 권.
리오넬은 망해가는 제국 한복판에서 누구보다 논리적으로, 누구보다 광기 어린 방식으로 권력을 재설계해나간다.
그는 주인공이 아니다.
그리고 세상은 그를 악역이라 불렀다.
“응. 내가 악역이라고?
그럼 진짜 악역이 뭔지 보여주지.”

  - **요한** :오랜 세월 백작가의 집사로 헌신하였던 한 사내가 있었다. 그의 이름은 요한이었다. 그는 주인의 신임을 한 몸에 받았으나, 그 신임이 도리어 화근이 되었다. 주인의 장자가 유산 상속의 비밀을 감추기 위하여 그를 제거하기로 결심한 까닭이었다. 요한은 저택의 지하에서 총성과 함께 쓰러졌고, 그것으로 그의 생은 끝난 줄로만 알았다. 그러나 그가 다시 눈을 떴을 때, 그는 백작가의 갓난 삼남으로 태어나 있었다. 그에게는 아직 도래하지 아니한 미래에 대한 기억이 남아 있었으니, 이는 그가 생전에 이미 은밀히 조사하여 알아낸 바였다. 그는 결심하였다. 이번 생에는 반드시 백작의 자리에 올라, 그를 배신한 자들로 하여금 마땅한 대가를 치르게 하리라고.

- **발견된 문제**:
  1. **(버그, 우선순위 높음)** `NaiveGenerationService`가 문단 수 검증 실패로 3회 재시도를 모두 소진한 뒤, 명확한 HTTP 오류를 반환하지 않고 응답이 멈춘 것으로 관찰됨 — 라이브 데모 중 발생 시 화면이 그대로 정지할 위험, 세션41에서 코드 확인·수정 필요
  2. **(태그 데이터 보강, 완료)** `metacognitive_dual_perspective`가 "고장난 시스템 메시지", "임무를 완수하세요", "루트를 따랐다" 같은 명백한 시스템/게임 프레임 표현을 놓침 — keywords에 "시스템 메시지"/"임무"/"루트"/"미션 완수" 추가
  3. **(태그 데이터 보강, 완료)** `informational_advantage_twist`의 근거가 "현대인의 냉철한 판단력"(성격 묘사)으로 잘못 귀속됨 — 실제 근거는 "이미 읽었던 소설 속 악역의 몸"이라는 책빙의 특유의 사전 지식이어야 함. psychological_function에 책빙의 사례 명시, keywords에 "읽었던"/"원작"/"이야기 속에서" 추가
  4. **(구조적 이슈, 보류)** `disguise_hidden_identity`가 "빙의"(다른 몸에 들어가 적응)와 "신분위장"(정체를 숨기고 위장, 발각 위험)이라는 서로 다른 성격을 하나로 묶고 있다는 문제 제기 — 태그를 9→10개로 늘리는 파급이 커서 이번엔 반영하지 않고 `향후_아이디어_메모.md`(아이디어4)에 기록
  5. 복수/응징, 영웅서사/시련과 희생, 정체성 모호성의 낮은 점수는 사용자도 "인간 기준으로도 애매하다"고 판단 — 명확한 버그로 보지 않고 조치 없음
- **완료한 작업**: `resources/data/tags.json`의 `informational_advantage_twist`, `metacognitive_dual_perspective` 두 항목 keywords·psychological_function 수정, `향후_아이디어_메모.md`에 아이디어4 추가
- **발견한 이슈/블로커**: 없음 (버그 1건은 다음 세션으로 이월)
- **변경된 파일**:
  - `resources/data/tags.json` (내 사본 — 실제 코드 반영은 세션41에서 sync 필요)
  - `향후_아이디어_메모.md`
  - `로그.md`
- **다음 세션 TODO**:
  1. tags.json 변경사항을 실제 `src/main/resources/data/tags.json`에 반영
  2. NaiveGenerationService(및 대칭 구조인 StructureGenerationService)의 재시도 소진 후 처리 로직 확인·수정 — 명확한 HTTP 오류 응답 보장
  3. 수정한 입력(리오넬 케이스)으로 재검증
---
 
## 세션 41 — Before 문단 초과 진단·복구 및 태그 재검증 (2026-07-24)

- **목표**: 리오넬·요한 Before 생성의 재시도 소진 원인을 실제 LLM 원문으로 판별하고, 명확한 오류 응답·태그 데이터 반영·고정 데모 회귀까지 확인
- **재현 입력**:
  - 리오넬: 세션40에 기록한 책빙의·시스템 루프 입력을 그대로 사용
  - 요한: 세션40에 기록한 백작가 집사 재탄생 입력을 그대로 사용
- **원인 진단**:
  - `NaiveGenerationService`가 검증 실패 시 LLM 원문 전체를 DEBUG 로그로 남기도록 추가하고, 수정 전 두 입력을 실제 `POST /api/generate-naive`로 호출
  - 리오넬은 3회 모두 실제 HTTP 502(31.8초), 요한도 3회 모두 실제 HTTP 502(35.3초)로 재현
  - 원문 6개를 확인한 결과 파서 오인이 아니었음. 모델이 대사·시스템 문구를 빈 줄로 각각 분리해 실제 5문단 이상을 생성함:
    - 리오넬 실패 지점: 장면2 6문단, 장면5 8문단, 장면3 5문단
    - 요한 실패 지점: 장면2 6문단, 장면1 6문단, 장면1 6문단
  - `paragraphCount`는 빈 줄로 구분된 실제 문단을 올바르게 세고 있어 파싱 로직은 변경하지 않음
- **수정 및 실제 확인**:
  - Before 프롬프트에 장면당 3문단 목표, 5문단 이상 전체 실패, `text`별 `\n\n` 1~3개, 대사·시스템 문구를 별도 문단으로 쪼개지 않는 규칙 추가
  - 수정 후 리오넬 HTTP 200(15.7초), 요한 HTTP 200(13.1초)으로 모두 첫 호출 성공
  - 두 응답 모두 장면 5개, 장면별 문단 수 `3/3/3/3/3`
  - 재시도 소진 시 서비스가 HTTP 502를 던지는 기존 동작을 테스트로 고정하고, 컨트롤러가 `{"message":"LLM이 3회 연속 유효한 Before 생성 JSON을 반환하지 않았습니다."}` 형태의 한국어 오류 본문을 반환하도록 처리
  - 프론트엔드가 백엔드의 한국어 `message`를 읽어 화면에 표시하도록 변경
- **태그 동기화 및 재진단**:
  - 루트 `tags.json`과 실제 런타임 `src/main/resources/data/tags.json`을 바이트 단위 비교해 동일함을 확인
  - 두 파일 모두 `informational_advantage_twist`에 책빙의 사전 지식 설명과 `읽었던`/`원작`/`이야기 속에서`, `metacognitive_dual_perspective`에 `시스템 메시지`/`임무`/`루트`/`미션 완수`가 반영된 상태
  - 진단 모델의 판정 흔들림을 막기 위해 입력 키워드 직접 대조와 해당 원문 인용을 `tag-diagnosis.txt`에 명시
  - 리오넬 최종 실제 진단 HTTP 200:
    - `informational_advantage_twist`: 85점, 활성, 근거 `"한때 자신이 읽었던 로맨스 판타지 속 악역 북부 대공의 몸에서 눈을 뜬다."`, `"그래, 원작처럼 행동하면 되는 거야?"`
    - `metacognitive_dual_perspective`: 80점, 활성, 근거 `"고장난 시스템 메시지 하나."`, `"악역의 임무를 완수하세요."`
- **회귀 검증**:
  - 상세기획문서 6번 고정 데모 `POST /api/generate`: HTTP 200(98.6초)
  - 장면 5개 및 단계 순서 `상실 → 회귀·각성 → 목표 설정 → 실행 → 응징·도달`, 문단 수 `2/2/2/2/2`
  - `verification.checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
  - `mvn -Dmaven.repo.local=/tmp/storylens-m2 clean test` 성공: 테스트 15개, 실패 0, 오류 0
  - 프론트엔드 `npm run lint`, `npm run build` 성공
- **발견한 이슈/블로커**:
  - 블로커 없음
  - 8080 포트는 기존 실행본이 사용 중이어서 사용자 프로세스를 건드리지 않고 최신 코드를 8081에서 검증
- **변경된 파일**:
  - `src/main/java/com/storylens/plot/NaiveGenerationService.java`
  - `src/main/java/com/storylens/plot/StructureGenerationController.java`
  - `src/main/resources/prompts/tag-diagnosis.txt`
  - `src/test/java/com/storylens/plot/NaiveGenerationServiceTest.java`
  - `src/test/java/com/storylens/tag/TagDiagnosisServiceTest.java`
  - `frontend/src/App.jsx`
  - `로그.md`
- **Git**:
  - `git add`, `git commit` 하지 않음
- **다음 세션 TODO**:
  1. 없음

---

## 세션 42 — 앨리스 계열 테스트: 고유명사 변경·단계라벨 편향 발견 및 문서 수정 (2026-07-24)
 
- **목표**: 복수/회귀 없는 순수 "낯선세계 적응" 케이스(앨리스 계열, 고전 문체)로 실브라우저 테스트, 발견된 문제 반영
- **테스트 입력**: 애그니스가 흰 토끼(족제비 아님, 사용자 입력 기준)를 좇다 토끼굴에 빠져 낯선 땅(그림자가 홀로 걷고 시간이 옆으로 흐르는 곳)에서 이름 세 번 부르면 사라지는 법도, 카드 병정, 체셔 고양이, 거꾸로 도는 다과회를 거쳐 여왕의 열쇠를 얻어 원래 정원으로 돌아가는 이야기 (복수/응징 요소 의도적으로 배제)
- **진단 결과**: `unfamiliar_world_adaptation`만 "강하게 감지"로 활성, 나머지 8개 태그 전부 DORMANT — 의도대로 낯선세계 적응 단독 케이스로 확인됨
- **발견된 문제**:
  1. **(버그)** After 생성 결과에서 사용자가 입력한 주인공 이름 "애그니스"가 "루미"로 임의 변경됨 — "[참조 서사 구조]의 원작 고유명사 복제 금지" 규칙을 모델이 과잉 적용해 참조 카드(alice_in_wonderland)의 고유명사뿐 아니라 **사용자 자신의 입력 고유명사**까지 바꿔버린 것으로 추정. 체셔 고양이→"고양이", 흰 토끼→"족제비"처럼 참조 카드의 고유명사는 의도대로 잘 피했으나, 그 규칙이 사용자 입력에는 적용되면 안 된다는 명시가 없었음
  2. **(구조적 편향)** `revenge_retribution` 태그가 완전히 비활성(DORMANT)임에도, 5단계 뼈대의 마지막 단계 이름이 "응징·도달"이라는 이유만으로 "여왕이 방문객의 이름을 훔쳐 가두었고, 주인공이 이를 돌려주어 해방시킨다"는 고정 조건에 없는 응징형 서브플롯이 임의로 창작됨 — 활성 태그가 아니라 단계 라벨의 의미론에 이끌린 편향으로 확인
  3. Actor-Evaluator 체크리스트는 7개 전부 PASS — 다만 3번 항목이 "배신·응징의 인과가 연결된다"고 평가한 것 자체가, 애초에 근거 없이 창작된 서브플롯을 사후적으로 정당화한 결과라는 한계가 있음(체크리스트가 "이 응징이 애초에 필요했는가"까지는 검증하지 않음 — 향후 고려사항)
- **완료한 작업** (대화 중 문서 반영, 코드 반영은 세션43에서 필요):
  - `resources/prompts/structure-generation.txt`: "[사용자 원본 설정]의 고유명사는 절대 바꾸지 말 것" 명시 추가, "응징·도달"은 라벨일 뿐이며 고정 조건에 없는 응징을 지어내지 말라는 규칙 추가
  - `prompts/02_structure_generation_prompt.md` 문서 사본도 동일하게 갱신
  - `상세기획문서.md` 3-2번 섹션에 이번 발견 두 가지 기록
  - `resources/prompts/tag-diagnosis.txt`를 세션41 최신 버전(키워드 직접 대조, 일관성 검사 규칙)으로 동기화
- **발견한 이슈/블로커**: 없음 (문서 반영은 완료, 실제 코드·런타임 리소스 반영 및 재검증은 다음 세션 필요)
- **변경된 파일** (내 사본 기준, 실제 저장소 반영은 세션43에서):
  - `resources/prompts/structure-generation.txt`
  - `resources/prompts/tag-diagnosis.txt`
  - `prompts/02_structure_generation_prompt.md`
  - `상세기획문서.md`
  - `로그.md`
- **다음 세션 TODO**:
  1. 위 문서 변경사항을 실제 `src/main/resources/prompts/structure-generation.txt`에 반영
  2. 애그니스 입력으로 재검증 — 이름이 그대로 유지되는지, 응징 서브플롯을 더 이상 임의로 만들어내지 않고 "귀환"으로 자연스럽게 마무리되는지 확인
  3. 톤 격리(참조 카드의 고전체가 새어나오는 문제)는 세션43 이후로 순연

  ---

## 세션 43 — 실행 단계 미니아크 확장 및 단계 편향 회귀 검증 (2026-07-25)

- **목표**: 사용자 고유명사 보존·응징 단계 라벨 편향 방지 규칙을 런타임에서 확인하고, 실행 단계를 4~6문단 미니아크로 확장
- **반영한 규칙**:
  - 세션42에서 추가된 다음 두 규칙이 실제 `src/main/resources/prompts/structure-generation.txt`에 이미 반영된 상태임을 확인하고 테스트로 고정:
    1. 참조 카드의 원작 고유명사 복제 금지는 참조 카드에만 적용하며 사용자 원본 설정의 고유명사는 유지
    2. `응징·도달`은 고정 라벨일 뿐이므로 복수·응징 조건이 없으면 응징 서브플롯 대신 입력의 실제 목표를 달성
  - 실행 단계만 4~6문단을 허용하고 나머지 네 단계는 기존 2~4문단을 유지하도록 생성 프롬프트와 서버 검증 로직 수정
  - 실행 단계에 `작은 목표 제시 → 시도 → 예상 밖의 위기·장애 → 극복 또는 부분적 성과` 미니아크를 압축하고, 정보우위가 있다면 예측이 흔들리는 순간을 포함하도록 명시
  - 미니아크 끝에 정보 획득·지위 상승·자원 확보처럼 눈에 보이는 성과 하나를 배치하도록 명시
  - 런타임 `src/main/resources/data/tags.json`의 `informational_advantage_twist.psychological_function`에 `"지식의 독점자"` 표현 동기화
  - JSON 스키마는 `scenes` 5개 그대로 유지하고 마스터플롯 단계도 늘리지 않음
  - 인과관계 검증과 충돌하는 `"우연성 제거"` 또는 `"개연성보다 사이다"` 지침은 추가하지 않음
- **애그니스 실제 재검증**:
  - 세션42 입력을 사용한 `POST /api/generate`: HTTP 200(65.8초)
  - 활성 태그는 `unfamiliar_world_adaptation` 하나, 다른 8개는 비활성
  - 주인공 이름 `애그니스`가 14회 유지되고 잘못 바뀌었던 이름 `루미`는 0회
  - 마지막 `응징·도달` 장면은 여왕 처벌·숨겨진 악당 같은 응징 서브플롯 없이 원래 정원으로 귀환
  - 장면별 문단 수 `2/3/2/5/2`; 실행 단계는 다과회의 열쇠 찾기 → 움직이는 잔과 이름 소멸 위기 → 그림자 활용 → 열쇠·통행패 확보의 미니아크
  - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
- **상세기획문서 6번 고정 데모 회귀 검증**:
  - `POST /api/generate`: HTTP 200(95.9초)
  - 활성 태그에 `revenge_retribution` 포함
  - 마지막 단계에서 현재 생의 비자금·인감 위조 증거로 장남 해임 및 후계자 등극이 생성되어, 복수 조건이 실제 있을 때는 응징 내용이 정상 유지됨
  - 장면별 문단 수 `3/3/3/5/3`
  - 실행 단계는 원자재 계약 조작 저지라는 작은 목표 → 미래 지식 활용 → 예상과 다른 대표 등장과 조작 누명 → 현장 단서로 극복 → 경영 교육·회의 자료 열람권 확보의 미니아크
  - `verification.checklist=7`, 7개 모두 PASS, `overall_pass_fail=PASS`, `regeneration_count=0`
- **추가로 발견·수정한 잠복 버그**:
  - 고정 데모 첫 호출에서 활성 태그의 `display_strength`가 누락된 LLM 응답을 `Set.of().contains(null)`로 검사하면서 NPE가 발생해 HTTP 500 반환
  - null을 유효하지 않은 진단 응답으로 판정해 기존 최대 3회 재시도 경로로 보내도록 수정하고, 같은 응답으로 NPE 대신 3회 재시도 후 502가 되는 회귀 테스트 추가
  - 이 오류는 1회 발생 후 수정되었고 같은 오류가 3회 반복되지 않음
- **테스트**:
  - `mvn -Dmaven.repo.local=/tmp/storylens-m2 clean test` 성공
  - 테스트 15개, 실패 0, 오류 0
- **발견한 이슈/블로커**:
  - 블로커 없음
- **변경된 파일**:
  - `src/main/resources/prompts/structure-generation.txt`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/resources/data/tags.json`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisService.java`
  - `src/test/java/com/storylens/tag/TagDiagnosisServiceTest.java`
  - `로그.md`
- **Git**:
  - `git add`, `git commit` 하지 않음
- **다음 세션 TODO**:
  1. 세션42에서 순연한 참조 카드의 고전 문체 누출 여부를 별도 검토

---

## 세션 44 — AOP 기반 파이프라인 실행 단계 로깅 (2026-07-25)

- **목표**: 리오넬 예시 재현 중 회귀·각성 장면 문단수 검증이 3회 연속 실패(5~6문단)했는데, 로그가 `StructureGenerationService`의 WARN 한 줄뿐이라 태그진단→카드선정→구조생성→Actor-Evaluator 중 어느 단계인지, 몇 번째 재시도·재생성 사이클인지 전혀 알 수 없었던 문제를 AOP로 해결
- **판단 근거**: `StructureGenerationController.generate()`(28~34행)는 `StructureGenerationService.generate()` 한 줄만 호출하고, 실제 4단계 파이프라인은 전부 그 메서드 내부에서 동기 실행됨. Filter/Interceptor는 컨트롤러 진입/이탈 1회만 보여 내부 단계를 구분 못 하므로 AOP(`@Around`)만 채택
- **완료한 작업**:
  - `pom.xml`에 AOP 스타터 의존성 추가 — 실제로는 `spring-boot-starter-aop`가 아니라 이 프로젝트가 쓰는 Spring Boot 4.1.0부터 이름이 바뀐 `spring-boot-starter-aspectj`였음(로컬 BOM에서 확인, `spring-boot-starter-aop`는 central에 4.0.0-M2까지만 존재)
  - `src/main/java/com/storylens/logging/PipelineExecutionAspect.java` 신설: `execution()` pointcut으로 `TagDiagnosisService.diagnose`, `CardSelectionService.select`, `StructureGenerationService.generate`, `ActorEvaluatorService.evaluate` 4곳을 감싸 `[단계명] 진입/완료(ms)/실패(ms)` 로그 기록, `StructureGenerationController.generate`를 감싸 요청 단위 `reqId`(MDC)를 채번·해제
  - `StructureGenerationService.generateOutline`에 `regenerationCycle` 파라미터를 추가해 기존 WARN 로그에 재생성 사이클 번호를 포함시키고, LLM 원본 응답을 `logger.debug`로 노출; 재생성 루프 진입 시 "재생성 사이클 N/2 시작(사유: ...)" INFO 로그 추가
  - `TagDiagnosisService`/`CardSelectionService`/`ActorEvaluatorService`에도 LLM 원본 응답 `logger.debug` 로그 1줄씩 추가 (재생성 개념이 없는 단일 루프라 파라미터 추가는 불필요)
  - `application.yml`에 `logging.pattern.console`(`%X{reqId}` 포함)과 `logging.level.com.storylens: INFO` 추가 — 이게 없으면 MDC의 reqId가 콘솔에 전혀 안 보임
  - `mvn compile`, `mvn test` 성공 (테스트 15개, 실패 0) — 기존 단위 테스트는 전부 `new XxxService(...)`로 컨테이너 없이 생성해 AOP 프록시 영향을 받지 않음을 확인
  - 리오넬 예시로 `POST /api/generate` 실제 호출: HTTP 200(86.5초), 콘솔 로그에서 `[f827a4a1]` 하나의 reqId로 `요청 시작` → `[구조생성] 진입` → `[태그진단] 진입/완료(42130ms)` → `[카드선정] 진입/완료(15289ms)` → `[액터평가] 진입/완료(11239ms)` → `[구조생성] 완료(86306ms)` → `요청 종료`까지 중첩 순서 그대로 확인
- **발견한 이슈/블로커**:
  - 블로커 없음. 다만 `StructureGenerationService.generateOutline()`이 `private`이고 같은 빈 안에서 self-invocation되어, AOP만으로는 내부 attempt 시도 횟수까지는 못 잡는다는 구조적 한계를 확인 — 이 부분은 AOP 대신 기존 코드에 파라미터를 직접 추가하는 방식으로 보완함
- **변경된 파일**:
  - `pom.xml`
  - `src/main/java/com/storylens/logging/PipelineExecutionAspect.java` (신규)
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/main/java/com/storylens/tag/TagDiagnosisService.java`
  - `src/main/java/com/storylens/cardselection/CardSelectionService.java`
  - `src/main/java/com/storylens/verify/ActorEvaluatorService.java`
  - `src/main/resources/application.yml`
  - `로그.md`
- **Git**:
  - `git add`, `git commit` 하지 않음
- **다음 세션 TODO**:
  1. 세션42에서 순연한 참조 카드의 고전 문체 누출 여부를 별도 검토
  2. 문단수 검증 실패가 재현될 때 새 로그로 실제 재생성 사이클·시도 번호가 정확히 찍히는지 한 번 더 실측 확인 (이번 세션의 리오넬 호출은 첫 시도에 성공해 재시도 경로 자체는 미가동)

---

## 세션 45 — 구조 생성 문단 수 검증 실패 복구 (2026-07-25)

- **목표**: 실행 단계 전용 4~6문단 미니아크 지시가 다른 네 단계까지 번져 5~6문단을 생성하는 문제를 막고, 문단 수 검증 실패 시 다음 시도에서 원인을 직접 교정하도록 복구
- **반영한 규칙**:
  - few-shot 예시를 정확히 3문단으로 축약하고, 대사와 시스템 알림을 각각 앞뒤 서술과 같은 문단에 포함
  - 실행 단계 미니아크 블록은 오직 4번 `실행` 단계에만 적용되며 다른 네 단계에는 적용하지 않는다고 명시
  - 각 `text`의 문단 구분은 `\n\n`만 사용하고, 실행 외 장면은 `\n\n` 1~3개·총 2~4문단으로 제한
  - 대사·시스템 문구를 별도 문단으로 쪼개지 않고, 실행 외 긴장 곡선은 2~4문단 안에서 압축
  - 출력 규칙을 실행 `4~6문단`, 나머지 네 단계 `3문단 목표·필수 2~4문단·5문단 이상 전체 실패`의 단계별 짧은 문장으로 분리
  - 5개 장면과 단계 순서·서버 검증 임계값은 유지
- **재시도 복구**:
  - 구조 생성 JSON/문단 검증 실패 시 다음 시도 프롬프트에 `[직전 시도 실패 사유 — 반드시 교정]`과 직전 예외 메시지만 추가
  - 매 시도는 기본 프롬프트에서 다시 만들므로 실패 사유가 누적되지 않음
  - Actor-Evaluator 재생성 교정은 `[Actor-Evaluator 교정 지시 — 반드시 반영]`으로 구분하고 최대 3회 시도·최대 2회 재생성은 유지
- **테스트**:
  - 첫 응답의 실행 외 5문단이 실패하고 두 번째 응답이 성공할 때, 두 번째 사용자 프롬프트에 `응징·도달 장면은 2~4문단이어야 합니다. 실제: 5`가 포함되는 테스트 추가
  - 실행 5문단은 통과하고 실행 외 5문단은 3회 실패 뒤 HTTP 502가 되는 테스트 추가
  - JDK 21에서 `mvn clean test` 성공: 테스트 17개, 실패 0, 오류 0
- **실제 HTTP 검증** (최신 코드를 8081에서 실행, 기존 8080 프로세스는 유지):
  - 리오넬(세션40 입력): HTTP 200(109.3초), 문단 수 `3/3/3/6/3`, `verification.checklist=7`, `overall_pass_fail=PASS`, `regeneration_count=0`
  - 애그니스(세션42 입력): HTTP 200(100.4초), 문단 수 `3/3/3/5/3`, 이름 `애그니스` 유지, `revenge_retribution` 비활성, 복수·응징 서브플롯 없이 원래 정원 귀환, 체크리스트 7개·전체 PASS·재생성 0회
  - 상세기획문서 6번 고정 데모: HTTP 200(110.4초), 문단 수 `3/3/3/5/3`, `revenge_retribution` 활성, 현재 생에서 실제로 저지른 횡령·조작에 근거한 해임·형사 처벌과 복수의 책임으로 마무리, 체크리스트 7개·전체 PASS·재생성 0회
- **판정**:
  - 세 입력 모두 실행만 4~6문단이고 나머지 네 단계는 2~4문단으로 통과
  - “미니아크 4~6문단 지시가 다른 단계에도 적용됐다”는 원인 가설과 일치하며, 범위 제한·few-shot 축약·재시도 교정으로 회귀가 해소됨
- **발견한 이슈/블로커**:
  - 블로커 없음
  - 고정 데모 첫 요청은 서버가 98.1초에 정상 완료했으나 Windows PowerShell의 `Invoke-WebRequest -OutFile -PassThru` 조합이 응답 객체 처리 중 null 오류를 내 원문을 저장하지 못함. 서버 오류가 아님을 로그로 확인하고 `curl.exe`로 재호출해 HTTP 200 응답 원문을 검증
- **변경된 파일**:
  - `src/main/resources/prompts/style-few-shot-example.txt`
  - `src/main/resources/prompts/structure-generation.txt`
  - `src/main/java/com/storylens/plot/StructureGenerationService.java`
  - `src/test/java/com/storylens/plot/StructureGenerationServiceTest.java`
  - `로그.md`
- **Git**:
  - `git add`, `git commit`, 태그 생성 하지 않음
- **다음 세션 TODO**:
  1. 세션42에서 순연한 참조 카드의 고전 문체 누출 여부를 별도 검토

---

## 세션 46 — 분석 대기 화면 단계별 진행 연출 (미완료, 2026-07-25)

- **목표**: 정적 스피너 대기 화면을 백엔드 추가 호출 없이 시간 기반 단계 표시·셔플백 보조 문구·경과 시간·정직한 완료/실패 시퀀스로 교체
- **완료한 작업**:
  - 동봉된 `loadingMessages.js` 문구와 단계 시간 `0/60/80/150초`를 변경하지 않고 사용
  - `useLoadingSequence` 훅 추가: 1초 경과 시간, 단계 전환, Fisher–Yates 셔플백, 대기열 경계 연속 중복 방지, 마지막 단계 소진 후 `OVERTIME_HINTS` 순환, 모든 interval/timeout 정리
  - `success`/`partial`은 남은 단계를 350ms씩 완료한 뒤 `FINALIZING_PHASE`를 600ms 표시하고, `error`는 현재 단계만 실패 처리한 뒤 800ms 후 전환하도록 구현
  - 3초 미만 응답은 완료 연출을 생략하되 로딩 패널을 최소 800ms 유지
  - `submit()`의 `Promise.allSettled` 병렬 구조와 180초 AbortController를 유지하면서 결과 화면 전환만 완료 콜백으로 이동
  - 단계 제목만 `aria-live="polite"`, 보조 문구는 `aria-hidden="true"`로 처리하고 `useReducedMotion()`에서 문구 전환 애니메이션 제거
  - 단계 상태를 `✓/●/○/✕`로 구분하고 좁은 화면에서 줄바꿈되는 CSS 추가
  - 연속 실행 직후 이전 실행의 마지막 보조 문구가 한 프레임 남는 문제를 발견해, 로딩 종료 시 훅 상태를 첫 단계 기본값으로 되돌리고 다음 시작 시 `useLayoutEffect`에서 셔플백을 초기화하도록 수정
  - 사용자 결정에 따라 시간 기반으로 150초에 `VALIDATING_GUARDRAILS`에 진입할 때는 일반 검증 힌트 대기열을 기다리지 않고 즉시 `OVERTIME_HINTS`를 사용. 응답 후 순차 완료 연출로 이 단계에 진입할 때는 기존 검증 힌트를 유지
  - 백엔드 파일은 변경하지 않음
- **검증**:
  - `npm run lint` 성공
  - 최종 수정 후 `npm run build` 성공: Vite 8.1.5, 421개 모듈 변환
  - `git diff --check` 내용 오류 없음(Windows CRLF 변환 경고만 존재)
- **실제 확인 내용**:
  - 인앱 브라우저 `http://127.0.0.1:5173`에서 단계 목록·경과 시간·보조 문구를 확인
  - 고정 데모: 첫 호출은 180초 타임아웃됐으나, 사용자 승인 후 1회만 재시도해 180초 안에 성공. `태그 진단 ● → 카드 선정 ● → 구조 생성 ● → 가드레일 검증 ● → 실제 단계 전체 ✓ + 결과 준비 ● → 진단 화면`의 전체 완료 시퀀스와 결과 화면의 오류 없음을 확인
  - 애그니스: 실제 성공 응답에서 `태그 진단 → 카드 선정 → 구조 생성` 전환 뒤 남은 실제 단계가 `✓` 처리되고 `결과 준비`가 `●`로 표시된 다음 진단 화면으로 이동하는 완료 시퀀스 확인
  - 접근성 DOM: 단계 제목 `aria-live="polite"`, 보조 문구 `aria-hidden="true"` 확인
  - 즉시 400: 공백 입력에서 첫 단계 패널이 최소 800ms 유지된 뒤 기존 한국어 오류 `input은 비어 있을 수 없습니다.`를 표시하고 결과 화면으로 이동
  - 진행 중 실패: 외부 LLM 호출 없이 검증하기 위해 실제 백엔드를 잠시 내리고 4초 지연 503 로컬 응답기를 사용. `✕ 태그 진단`만 실패 처리되고 이후 단계와 결과 준비는 모두 `○`로 유지된 상태를 연속 관찰했으며, 약 800ms 뒤 기존 오류 표시 경로로 전환됨을 확인
  - 실패 화면 전환 7.2초 뒤에도 로딩 패널과 보조 문구가 다시 나타나지 않고 브라우저 경고·오류 로그가 없어 타이머 정리를 확인
  - 연속 실행에서 첫 단계 초기 문구가 이전 실행 문구를 재사용하지 않음을 최종 새로고침 후 확인
  - 같은 단계의 보조 문구 순서가 실행마다 달랐고 관찰 구간에서 연속 중복은 없었음
  - 오버타임 검증을 위해 마지막 단계 `startAt`을 임시로 5초로 낮춤: 5초 이후 `OVERTIME_HINTS`가 표시되고 7초 뒤 다른 문구로 교체되며, 가드레일 검증은 `●`, 결과 준비는 `○`로 유지됨을 확인. 검증 직후 원래 값 150초로 복원
  - 테스트를 위해 중단했던 백엔드는 JDK 21로 8080에 재기동(PID 22008)
- **추가 수정 (Codex 토큰 소진 후 이어서 직접 작업)**:
  - `VALIDATING_GUARDRAILS` 진입 시 무조건 `OVERTIME_HINTS`를 사용하던 로직 제거. 세션45 실측(리오넬 109.3초·애그니스 100.4초·고정 데모 110.4초)에서 150초에 도달하는 케이스가 없어, 가드레일 검증 힌트 9개가 정상 케이스에서 한 번도 표시되지 않는 문제였음
  - 단계 진입 시점과 오버타임 판정을 분리. `startAt`을 `0/50/70/95초`로 재조정하고 `OVERTIME_AFTER_SECONDS = 140`을 신설해 오버타임은 단계와 무관하게 경과 시간만으로 판단
  - 완료 시퀀스의 350ms 순차 완료를 제거. 응답 도착 시 남은 단계를 한 번에 `✓` 처리하고 `결과 준비`를 500ms 표시한 뒤 전환(최대 2.0초 → 0.5초)
  - 실패(`error`)는 기존대로 현재 단계만 `✕` 처리하고 이후 단계는 `○` 유지, 3초 미만 응답은 연출 생략 후 최소 800ms 유지 — 두 규칙 모두 변경 없음
  - 대기 문구를 `보통 2~3분 정도 걸립니다` → `보통 2분 안팎이 걸립니다`로 실측에 맞춰 수정
- **추가 검증**:
  - `node --check` 통과, ESM 모듈 그래프 로드 및 export 확인(`OVERTIME_AFTER_SECONDS = 140`, 단계 `0/50/70/95`)
  - 타이밍 시뮬레이션(100초·110초·180초·45초)으로 네 단계 전환, 140초 오버타임 전환, 실패 시 미도달 단계 미표시, 보조 문구 연속 중복 0회를 확인. 100초·110초 케이스 모두에서 `가드레일 검증` 단계가 실제로 표시됨
  - `npm run lint` / `npm run build`는 `node_modules`의 oxlint·rolldown 네이티브 바이너리가 Windows 전용이라 Linux 환경에서 실행 불가 — Windows에서 재실행 필요
- **발견한 이슈/블로커**:
  - 부분 실패와 동작 줄이기는 실제 화면에서 최종 확인하지 못함
  - Windows에서 `npm run lint` / `npm run build` 재실행이 남아 세션46은 미완료
  - 저장소 전체가 Windows `core.autocrlf=true` 체크아웃 상태(작업 트리 CRLF / blob LF)임을 확인. 세션46이 유발한 것이 아니고 Windows에서는 `git status`가 정상이지만, 다른 환경에서 저장소를 열면 모든 파일이 수정된 것처럼 보임
  - 세션46 중 고정 데모 첫 호출이 180초 타임아웃. 세션45 실측 110.4초 대비 편차가 커 데모 안정성 확인 필요
- **변경된 파일**:
  - `frontend/src/loadingMessages.js` (동봉 파일)
  - `frontend/src/useLoadingSequence.js` (신규)
  - `frontend/src/App.jsx`
  - `frontend/src/App.css`
  - `로그.md`
- **Git**:
  - `git add`, `git commit`, `git tag` 실행하지 않음
- **다음 세션 TODO**:
  1. Windows에서 `npm run lint` / `npm run build` 실행 확인
  2. 부분 실패와 동작 줄이기를 실제 화면에서 확인한 뒤 세션46을 완료 상태로 갱신
  3. 고정 데모 180초 타임아웃 재발 여부 확인 — 세션44 AOP 로그로 단계별 소요 시간 대조
  4. 세션42에서 순연한 참조 카드의 고전 문체 누출 여부를 별도 검토
