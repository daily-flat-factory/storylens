# StoryLens 작업지침

> Codex CLI(랄프 루프)가 세션마다 일관된 기준으로 개발하도록 안내하는 문서.
> `상세기획문서.md`(무엇을 만들지)와 `로그.md`(무엇을 했는지)를 항상 함께 참조할 것.

---

## 1. 세션 운영 규칙

- **세션 시작 시**: `상세기획문서.md` 전체 + `로그.md`의 마지막 세션 "다음 세션 TODO"를 컨텍스트로 제공
- **세션 시작 직전 git 동기화 확인 (여러 기기를 오가며 작업할 경우 필수)**: `git fetch origin && git status`로 로컬이 origin과 어긋나 있지 않은지 먼저 확인할 것. 어긋나 있으면 커밋하지 않은 로컬 변경사항이 있는지 확인 후 `git reset --hard origin/main`(및 필요 시 `git clean -fd`, `-x` 옵션은 `.env` 삭제 위험이 있어 사용 금지)으로 origin 기준으로 맞추고 세션을 시작할 것. 이걸 건너뛰면 로컬 HEAD가 뒤처진 상태에서 새 세션이 시작되어, 이전 세션 코드가 없다고 판단한 에이전트가 git 없이 파일을 임의로 복사해 넣는 등 저장소 상태가 꼬일 수 있음
- **세션 목표는 1개로 좁게 유지** — 여러 목표를 한 세션에 동시 진행하지 말 것 (삽질 확률 증가)
- **세션 종료 시**: `로그.md`에 반드시 기록 (목표 / 완료한 작업 / 발견한 이슈·블로커 / 변경된 파일 / 다음 세션 TODO)

## 2. Definition of Done (세션 완료 기준)

세션은 아래 4가지를 모두 만족해야 "완료"로 본다:
1. 컴파일/빌드 에러 없음
2. 해당 세션 목표 기능이 최소 1회 수동 테스트로 동작 확인됨
3. 변경사항이 `로그.md`에 기록됨
4. 실패한 부분이 있다면 무리하게 계속 시도하지 않고, 다음 세션 TODO에 구체적으로 이슈를 명시한 뒤 세션 종료

## 3. 코딩 컨벤션

- **백엔드**: Spring Boot + Spring AI, **Maven**(Gradle 아님 — 7번 참조), 표준 Java 컨벤션(camelCase), 패키지 구조 `com.storylens.*`
- **프론트엔드**: React + Tailwind CSS, 컴포넌트 단위로 분리, Tailwind 유틸리티 클래스 우선 사용, 애니메이션은 Framer Motion
- **커밋 단위**: 기능 단위로 작게 — 세션당 1~3커밋 권장
- **커밋 메시지**: `[세션N] 요약` 형식
- **커밋/태그 실행 주체 (중요 — Codex 샌드박스 알려진 버그)**: Codex CLI의 `workspace-write` 샌드박스가 `.git` 디렉토리를 읽기 전용으로 막아 `git commit`이 `.git/index.lock` 생성 실패로 거부되는 경우가 있음(Codex CLI 자체의 널리 보고된 미해결 이슈, OS 무관). 이를 우회하려고 `--dangerously-bypass-approvals-and-sandbox`로 전체 샌드박스를 푸는 것은 권장하지 않음(불필요하게 광범위한 권한 노출). **대신 세션 끝에서 Codex는 파일 준비와 로그.md 기록까지만 하고, `git add`/`git commit`/`git tag`는 사용자가 직접 실행할 것.** (세션 프롬프트에 이 지침 포함할 것)
- **브랜치/태그 전략**: 세션별 브랜치 전환은 하지 않음 — 세션이 순차적으로 이전 세션 위에 쌓이는 구조라 브랜치 분기의 이점이 없고 전환·머지 오버헤드만 늘어남. 대신 **단일 브랜치에 계속 커밋 + DoD(2번) 통과한 세션마다 가벼운 git 태그**(`git tag session-N`)를 남길 것. 태그가 있으면 `git diff session-13..session-14`로 특정 세션 변경분을 확인하거나, 문제 발생 시 `git reset --hard session-N`으로 안전하게 롤백 가능. DoD 미달 세션은 애초에 커밋하지 않고 로그에 블로커만 남기므로(5번) 태그는 항상 "정상 동작 확인된 지점"만 가리키게 됨

## 4. 스코프 가드레일 (반드시 지킬 것)

- `상세기획문서.md` 4번(MVP 범위)의 **out of scope 항목을 임의로 추가하지 말 것** (완성 단편 전체 텍스트 생성, 다중 입력 케이스 대응, 사용자 계정/로그인, 서버 측 영속 저장·공유 기능, 트로프 태그 자동 확장 등). 단, **클라이언트 사이드 결과 다운로드는 필수 기능**이며 위 제외 항목(서버 저장·공유)과 다름 — 혼동하지 말 것
- **저작권 있는 실제 웹소설 텍스트**를 few-shot이나 참조 데이터로 사용 금지 — 반드시 5-2의 오리지널 few-shot 예시 또는 구텐베르크(퍼블릭 도메인) 텍스트만 사용
- **트로프 태그 개수(9개)를 임의로 확장/축소하지 말 것** — 태그 풀 변경은 사람이 먼저 `상세기획문서.md` 5번을 갱신한 후에 진행
- **6번의 고정 데모 입력값과 예상 태그 매칭 결과는 개발 중 임의로 변경하지 말 것** (다른 테스트 케이스는 자유롭게 추가 가능)
- **태그 배치 로직(3-1)은 "순위"가 아니라 "단계별 위치 배정" 방식임을 항상 유지** — 상위 몇 개만 남기고 나머지를 장식으로 격하하는 방식으로 되돌아가지 말 것
- **생성 시 "고정 조건"(3-2의 핵심목표/필수사건/지속조건/톤조건)과 "자유 창작 영역"을 분리할 것** — 전부 고정하면 매번 같은 결과물이 나오는 반복성 문제가 생김. 장면 연출·대사·디테일은 고정 조건을 어기지 않는 한 매 생성마다 다르게 창작하도록 프롬프트에 명시할 것
- **자체 검증(Actor-Evaluator, 3-3)은 반드시 체크리스트 기반으로 구현할 것** — "아크와 일치하는가?" 같은 추상적 프롬프트 금지. PASS/FAIL + evidence + violated_constraint + correction_instruction을 JSON으로 산출할 것. 자유 창작 영역의 변주는 검증 대상에서 제외
- **Before/After 데모 비교는 같은 모델·입력·출력길이·temperature로 통제할 것(3-4)** — Before를 일부러 부실하게 만들지 말 것. 차이는 오직 StoryLens의 구조 설계·검증 적용 여부에서만 나야 함
- **태그 진단 점수를 사용자 화면에 원점수(0~100, +30 등)로 그대로 노출하지 말 것** — "강하게 감지 + 근거 문구" 형태로 표시(3-1 참조). 내부 로직에서는 원점수를 계속 사용해도 됨
- **구텐베르크 원문 전체를 청크 단위로 임베딩하지 말 것** — narrative_cards.json의 구조화된 서사 카드를 태그 필터링 + LLM 비교에 사용할 것(3-1-1 참조). 원문 전체 임베딩은 장면 레벨 유사성만 찾고 작품 전체 플롯 구조를 포착하지 못함
- **생성 시 참조 서사 카드의 `usage_constraints`를 반드시 확인하고, 원작의 고유 인물명·사건·구체적 문장을 복제하지 말 것** — 추상적 구조 패턴만 참고
- **LLM 연동은 `spring-ai-starter-model-openai` + Gemini OpenAI 호환 엔드포인트로 시작할 것(7번 참조)** — `application.yml`에 `project-id`나 `location`을 절대 넣지 말 것(무료 API 키가 유료 Vertex AI 모드로 전환되어 거부됨). 다른 프로바이더 스타터는 실제 교체 시점 전까지 추가하지 말 것
- **API 키를 코드나 `application.yml`에 직접 하드코딩하지 말 것** — 환경변수 또는 `.gitignore`에 이미 등록된 `application-local.yml`/`.env`로 분리할 것 (`.gitignore` 참조)

## 5. 에러/블로커 처리 원칙

- 같은 에러에 **3회 이상 반복 실패 시**, 임의로 우회 구현하지 말고 `로그.md`에 블로커로 명확히 기록한 뒤 세션 종료 — 사람 판단을 기다릴 것
- LLM 프롬프트 응답(JSON) 파싱 실패는 재시도 로직으로 처리하되, **무한 재시도 루프 방지** (최대 재시도 횟수 설정)
- 완성도(심사기준 25점) 리스크 관리를 위해 모든 LLM 호출에는 폴백 처리를 기본으로 둘 것

## 6. 파일 구조 제안 (확정 — 실제 배치 경로)

> `.md`로 작성된 `tags.json`/`narrative_cards.json`/`prompts/00~03_*.md`는 **사람이 읽는 설명·예시 문서**임 (근거·구현참고·예상출력 포함). 실제 애플리케이션이 로드하는 파일은 아래처럼 순수 데이터/텍스트로 분리된 버전을 사용할 것.
>
> **시점 구분 주의**: 아래 트리는 **Maven 스캐폴딩 완료 후 최종 상태**를 보여줌. 현재 `storylens_project_seed.zip`(개발 착수 전 준비물 묶음)에는 `pom.xml`, `src/main/java/`, `frontend/`가 아직 없고(랄프 루프가 스캐폴딩하며 생성할 것), 대신 zip 최상위에 `resources/`(→ 스캐폴딩 후 `src/main/resources/`로 그대로 복사), `prompts/`, `reference/`, `.gitignore`, 문서 3종(상세기획문서.md/로그.md/작업지침.md), `향후_아이디어_메모.md`가 들어있음. 즉 zip은 "재료", 아래 트리는 "완성 형태" — 스캐폴딩 시 `resources/` 내용물을 `src/main/resources/`로 옮기면 됨.

```
storylens/
├── pom.xml               # Maven 빌드 설정 (spring-ai-bom 임포트 — 7번 참조)
├── src/main/java/com/storylens/
│   ├── cardselection/  # 참조 서사 카드 선정 (3-1-1)
│   ├── tag/             # 태그 진단 로직 (3-1)
│   ├── plot/            # 구조 고정 생성, 단계 배치·기능유형 로직 (3-1, 3-2)
│   ├── verify/           # 자체 검증 (Actor-Evaluator, 3-3)
│   └── export/           # 결과물 다운로드 기능 (7번 참조)
├── src/main/resources/
│   ├── data/
│   │   ├── tags.json              # 9개 태그 데이터 (5번 참조)
│   │   └── narrative_cards.json   # 12개 참조 작품 서사 카드 (3-1-1 참조)
│   └── prompts/
│       ├── card-selection.txt       # 프롬프트 0 순수 텍스트
│       ├── tag-diagnosis.txt        # 프롬프트 1 순수 텍스트
│       ├── structure-generation.txt # 프롬프트 2 순수 텍스트
│       └── actor-evaluator.txt      # 프롬프트 3 순수 텍스트
├── frontend/
│   └── src/components/  # 입력 / 진단결과(상태창 UI) / Before-After / 다운로드 버튼
├── .gitignore
├── 상세기획문서.md
├── 로그.md
├── 작업지침.md
├── 향후_아이디어_메모.md   # MVP 범위 밖 아이디어 보관 (개발에 직접 관여 안 함)
└── (참고용 설명 문서 — 실제 로드 안 함)
    ├── tags.json (설명용, .md 아님 — data/tags.json과 내용 동일)
    ├── narrative_cards.json (설명용 — data/narrative_cards.json과 내용 동일)
    ├── prompts/00~03_*.md (설명용 — resources/prompts/*.txt가 실제 사용 버전)
    ├── reference/ (조사한 학술논문 발췌본 — 예: 안상원_2018_마스터플롯연구.md, 개인 연구 참고용)
    └── before_after_draft.md
```

**{{placeholder}} 처리**: `resources/prompts/*.txt`의 `{{...}}` 자리표시자는 Spring AI의 `PromptTemplate` 또는 단순 문자열 치환으로 런타임에 실제 값을 주입할 것.

## 7. 우선순위 요약 (일정 계획 — 상세기획문서.md 9번과 동기화, 참고용)

| 날짜 | 목표 |
|---|---|
| 7/23 | 프로젝트 스캐폴딩 (Spring Boot+AI 초기화, React 스캐폴딩, tags.json 작성) |
| 7/24 | 태그 진단 로직(3-1) 구현 — 이 기한까지 React 연동이 꼬이면 Thymeleaf/정적 화면으로 하향 전환 |
| 7/25~7/26 | 단계 배치(3-1) + 기능유형/자유창작영역(3-2) 반영한 구조 고정 생성 프롬프트, 자체 검증(Actor-Evaluator, 3-3) 구현 |
| 7/27~7/28 | 프론트엔드 3화면 + RPG 상태창 UI + 애니메이션 |
| 7/29 | 기능 동결, 데모 케이스 100% 안정화, 내부 QA 테스트 3종 실행 (9-1) |
| 7/30 | 시연영상 촬영/편집, 출품작 설명서 작성 |
| 7/31 | 제출 (마감 14:00, 오전 중 제출) |

> 이 표는 `로그.md`의 실제 세션 진행 상황에 따라 갱신될 수 있음 — 고정 스펙이 아니라 참고용 로드맵. 날짜 기준은 상세기획문서.md 9번과 항상 동기화할 것.